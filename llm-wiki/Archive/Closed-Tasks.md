# 종료한 과제 기록

`Next-Tasks.md`에서 분리한 종료 기록. **내용은 원문 그대로이며 요약하지 않았다.**

훅은 `Next-Tasks.md`의 `### FE-`·`### BE-` 줄만 읽으므로 이 파일은 주입에 관여하지 않는다 —
"저 과제는 왜 그렇게 끝났나"를 확인할 때만 연다.

열린 과제: [[Next-Tasks]] · 사람이 읽을 연표: [[Summaries/History]] · 지도: [[index]]

## 표

| # | 과제 | 결과 | 정본·근거 |
|---|---|---|---|
| FE-42 | 로그인 사용자가 만든 이벤트의 브랜드명이 전부 "새 이벤트"로 박히고, 고칠 방법이 없다 | **완료.** 원인은 타입 구조였다 — `brand`가 `EventItem`에만 선언돼 있어 에디터의 모든 필드 편집이 거치는 `patchEvent()`(`Partial<EventDetail>`)로는 애초에 건드릴 수 없었다. `brand`를 `EventDetail`로 옮기고(`EventItem`은 상속으로 계속 가짐), `detailPatchToBody`에 `brand` 델타 매핑을 추가하고(`PATCH /api/events/[id]`는 이미 `brand`를 받고 있어 **BE 변경 0**), `FIELD_DEFS`(에디터 기본 정보 섹션이 그대로 렌더하는 목록)에 "브랜드명"을 행사명 바로 다음에 추가 — 기존 title·venue와 완전히 같은 저장 경로를 그대로 탄다. **생성 시점 자체는 그대로 뒀다** — `POST /api/events`가 빈 문자열을 필수 위반으로 거절해서(`app/api/events/route.ts:143`) 로그인 생성은 여전히 제목을 brand로 임시 채우고, 완료 기준이 허용하는 "최소한 이어서 고칠 수 있는 경로"로 충분하다고 판단했다. **이미 "새 이벤트"로 박힌 기존 행의 처리 방침** — 마이그레이션하지 않고 화면에서 고치게 둔다(로컬 dev 데이터라 실제 의도된 브랜드명을 추측할 방법이 없고, 이 과제가 "고칠 수단"을 만드는 게 목적이라 수단이 생긴 이상 과거 값을 자동으로 바로잡을 근거가 없다). "새로 만든 행사의 참가자 화면에 그 값이 나온다"는 완료 기준은 **FE-37이 아직 안 끝나 참가자 화면이 `brand`를 안 읽는 상태라 지금은 검증 대상이 아니다** — FE-37이 `events.brand`를 읽도록 고치는 순간 이 필드가 이미 올바른 값을 들고 있으므로 자동으로 만족된다. **실측**(로컬 D1에 가짜 세션을 심어 재현 — FE-41 ③과 같은 방식): 로그인 세션으로 새 이벤트를 만들고 브랜드명을 고치니 700ms 디바운스 뒤 `PATCH`가 나가 "방금 저장됨"으로 바뀌었고, `GET /api/events/[id]`로 서버 반영 확인, 하드 리프레시 후에도 값이 남았다. 게스트(서버 미연결) 이벤트에서도 같은 필드로 로컬 수정이 콘솔 에러 없이 됨을 확인. 테스트 이벤트·세션은 지웠다. `npm run lint`·`build`·`test`(168건) 통과 | `lib/{types,data,studio-api}.ts`, [[log]] 2026-09-22 |
| BE-33 | 정본 도메인(`sympo.superjacob.com`)이 Google OAuth 승인 리디렉션 URI에 등록돼 있지 않음 | **완료(콘솔 설정, 코드 변경 0).** 사용자가 "백엔드에 구글 로그인 등록이 됐는지"를 물어 점검하다 발견했다. **네 층으로 갈라 봤더니 세 층은 멀쩡했다** — ① 코드(`app/api/auth/google`·`/api/auth/callback/google`·`lib/auth.ts`·`0010_auth.sql`) ② 비밀값(`wrangler secret list`에 `GOOGLE_CLIENT_ID`·`GOOGLE_CLIENT_SECRET` 등록, 로컬 `.dev.vars`도 동일) ③ 배포된 엔드포인트(두 호스트 모두 `GET /api/auth/google` → 302 + `accounts.google.com` — `googleConfig`가 비밀값 없으면 503을 던지므로 **302 자체가 런타임 주입의 증거**다). **깨진 건 ④ Google Cloud Console 쪽 등록이었다.** `redirect_uri`만 바꿔 authorize 엔드포인트를 직접 호출해 비교하니 `sympo.superjacob.com`만 `redirect_uri_mismatch`(오류 페이지의 base64 `authError`를 디코드해 확인)이고 `workers.dev`·`localhost:3000`은 정상 로그인 화면으로 갔다 — **BE-22가 정본 주소를 커스텀 도메인으로 옮길 때 승인된 리디렉션 URI를 같이 추가하지 않았다.** README·[[Context]]·[[Summaries/Status]]가 가리키는 첫 주소에서 로그인이 시작조차 못 하던 상태다. **고친 방법**: 콘솔에서 해당 클라이언트(`371083668622-bn6ojbqt…`)의 승인된 리디렉션 URI에 `https://sympo.superjacob.com/api/auth/callback/google`을 추가했다(**기존 둘은 유지** — workers.dev를 살려 둔 것과 같은 이유, `wrangler.jsonc` 주석). **실측**: 반영 직후 같은 authorize 호출이 `…/signin/oauth/error`에서 **`…/v3/signin/identifier`**로 바뀌었고, 응답 쿼리에 **`app_domain=https://sympo.superjacob.com`**이 붙었다(Google이 그 도메인을 이 클라이언트의 것으로 인정했을 때만 생긴다). 이어서 **사용자가 정본 도메인에서 실제 계정으로 로그인 성공을 확인**했다(2026-09-21). **남은 앎의 한계**: OAuth 동의 화면의 게시 상태(「테스트」/프로덕션)와 테스트 사용자 목록은 따로 확인하지 않았다 — 사용자 계정으로는 `access_blocked` 없이 통과했으므로 **이 계정에 한해** 문제가 없다는 것까지만 확인된 것이고, 제3자 로그인이 필요해지면 그때 게시 상태를 봐야 한다. **교훈**: [[0007-sso-and-account-model]]이 "Google Cloud Console에서 redirect URI를 등록하는 **사람 손 작업**이 배포 절차에 추가된다"고 예고한 바로 그 항목이 실제로 누락됐다 — **도메인을 옮기는 작업(BE-22)의 완료 기준에 이게 없었다.** 다음에 호스트를 추가·변경하면 리디렉션 URI 등록을 같은 과제 안에서 함께 본다 | `wrangler.jsonc`, [[0007-sso-and-account-model]], [[log]] 2026-09-21 |
| FE-15 | 로그인 기반 + 게스트 구분 + 콘솔을 실제 D1 이벤트에 연결(범위 축소판) | 완료(게스트 경로 실측, 로그인 경로는 미실측). **①게스트 로컬 워크스페이스**: `StudioProvider.tsx`에 마운트 이펙트를 추가해 `GET /api/auth/me`로 로그인 여부를 먼저 확인하고, 비로그인이면 `localStorage`(`sympo-guest-events-v1`)에서 워크스페이스를 읽어온다(없으면 기존 시드 그대로 시작하고 이제부터 저장). 하이드레이션 불일치를 피하려고 **SSR·첫 클라이언트 렌더는 항상 시드로 시작**하고, 실제 게스트 데이터 교체는 마운트 이펙트(클라이언트 전용) 안에서만 한다. 로컬 이벤트 생성 시 `localRef`(`crypto.randomUUID()`)를 심어 FE-39가 나중에 멱등 가져오기를 얹을 수 있게 했다. **②로그인 버튼/계정 메뉴**: 사이드바 하단의 정적 "OP" 플레이스홀더를 실제 `components/AccountMenu.tsx`로 교체 — 게스트는 `/api/auth/google?next=<현재 경로>`로의 전체 페이지 이동 링크(원형 화살표 버튼), 로그인 사용자는 아바타(이니셜 또는 구글 프로필 사진)+드롭다운(이름·이메일·로그아웃)을 보여준다. **③게스트 배너**: `ConsoleScreen.tsx`에 "체험 중 · 이 브라우저에만 저장됩니다"를 추가(로그인 확인 전에는 안 뜬다 — `authStatus==='checking'`일 때 어느 쪽도 단정하지 않는다). **④콘솔을 실제 D1에 연결**: `lib/studio-api.ts`에 `fetchCurrentUser`·`fetchStudioEvents`·`createStudioEvent`·`logoutStudioUser`를 추가했다(전부 이미 완료된 BE-12·13·25 엔드포인트를 부르는 순수 배선). `createEvent()`를 컨텍스트 액션으로 새로 만들어 `StudioShell.tsx`의 "새 이벤트" 버튼이 로그인 여부로 갈라지게 했다 — 로그인이면 `POST /api/events`(서버가 필수로 요구하는 `brand`는 이 범위에 입력 필드가 없어 제목으로 대신 채움, 알려진 임시 결정), 게스트면 기존 로컬 전용 생성 로직 그대로(이번에 `localRef`만 추가). **실측(게스트, 브라우저)**: 새 이벤트 생성 → 콘솔 15→16건 → **하드 리프레시(`location.reload()`) 후에도 16건 유지**(사용자가 최초로 지적한 바로 그 증상 해소 확인) → 콘솔에서 다시 열어 편집 화면 정상 진입 확인. 다크모드에서도 배너·계정 위젯 정상 렌더 확인. 콘솔 에러 0건(`GET /api/events/{Date.now() id}`가 "id는 양의 정수여야 합니다" 경고를 남기지만 `isKnownLocally` 가드가 이미 잡아 화면엔 영향 없음 — FE-30 때부터 있던 기존 경로). **미실측**: 로그인 상태의 생성·목록 왕복(`POST`/`GET /api/events`), 다른 계정으로 로그인 시 격리, 로그아웃 후 게스트 워크스페이스 복귀 — 이 샌드박스에 실제 Google 계정 왕복 수단이 없다. 코드·타입 검사(`npm run build`)와 서버 계약(BE-12·13·25 라우트 직접 재확인)으로 대신했다. `npm run lint`·`build` 통과 | `components/{StudioProvider,StudioShell,AccountMenu}.tsx`, `components/screens/ConsoleScreen.tsx`, `lib/studio-api.ts`, `lib/types.ts`, [[log]] 2026-09-21 |
| FE-28 | doc_view 로그 중복 방지 | **기각(판단으로 종료, 코드 변경 없음).** PR #48을 열어 `Microsite.tsx`에 `seenDocsRef` 억제를 구현했으나, 팀원(fomula91)이 PR 코멘트로 전제 자체가 틀렸음을 지적했다 — **①** 운영자 리포트(`app/[slug]/report/page.tsx`)는 `ops.visitors`와 세션별 `row.visitors`만 읽는다. `hits`·`documents[]`는 화면 어디에도 안 나온다 — 중복 로그가 쌓여도 운영자가 보는 숫자는 하나도 안 변한다. **②** `visitors`는 이미 `COUNT(DISTINCT visitor)`라 SQL에서 중복이 제거된다(`lib/ops.ts`). **③** `hits`는 부풀어 있던 게 아니라 애초에 "몇 번"을 재도록 설계된 별개 지표다(같은 파일 주석이 "몇 명"·"몇 번"을 의도적으로 나눠 준다고 명시) — 이 PR을 머지하면 `hits ≈ visitors`가 되어 두 컬럼을 나눈 의미가 사라진다. **④** `session_view`가 `seenSessionsRef`로 억제하는 이유(`IntersectionObserver` 기반이라 스크롤 몇 번에 수십 건이 찍히는 구조)와 `doc_view`(사용자가 명시적으로 클릭한 횟수, 같은 폭증 없음)는 성격이 달라 패턴을 맞출 근거가 없다. 참가자가 강의 중 같은 자료를 반복해서 여는 것도 억제할 잡음이 아니라 "어떤 자료가 반복 참조되는가"라는 유효한 신호다. **코드로 재확인 후(huni2) 동의, PR #48은 머지하지 않고 닫았다.** **재검토 트리거**: `doc_view`가 로그 rate limit(BE-5, Q&A보다 넉넉함)을 실제로 쳐서 다른 로그를 유실시키는 경우 — 클릭 단위라 닿기 어려워 보여 지금은 조치 안 함. `hits`를 화면에 노출하기로 결정해도(현재 리포트에 자료 섹션 자체가 없음) 이 판단은 유지된다("열람 횟수"라는 정의 자체가 바뀌지 않는 한). PR이 close라서(merge 아님) 그 안의 위키 수정은 반영되지 않았고, 이 커밋이 그 판단을 별도로 기록한다 | [PR #48 코멘트](https://github.com/fomula91/sympo-studio/pull/48#issuecomment-5755374323) ([[log]] 2026-09-21) |
| FE-35 | 에디터가 보여주는 "생성될 URL"이 실제 저장된 주소와 다를 수 있다 | 완료. **무엇** — `EditorScreen.tsx`의 `BasicSection`이 `ev.slug`(실제 이벤트의 슬러그, 항상 존재)를 두고 매 렌더 `autoSlug(ev.title, ev.venue, ev.date)`를 독립적으로 재계산해 보여주고 있었다. `StudioProvider.tsx`의 `patchEvent`는 서버 이벤트(`isServerEvent`)면 slug를 절대 로컬에서 재계산하지 않는다(`PATCH` 계약이 "slug는 여기서 안 바꾼다"라서) — 즉 제목을 고치면 **화면에 보이는 URL만 바뀌고 실제 공개 주소는 그대로**라, 운영자가 화면을 보고 믿는 주소와 실제로 열리는 주소가 어긋날 수 있었다. 부수로 도메인도 `sympo.studio`로 하드코딩돼 있었는데 실제 배포 도메인은 BE-22 이후 `sympo.superjacob.com`이다(README 라이브 데모 링크와 대조해 확인) — 함께 정정. `sympo.superjacob.com/{ev.slug}`로 교체하고 이제 쓰지 않는 `autoSlug` import를 지웠다. **왜** — 외부 다각도 리뷰(프론트/플로우/PM/개발자/사용자 관점)가 지적, 코드로 재확인. `ev.slug`는 로컬 전용 이벤트에서는 `uniqueSlug`로 계속 재계산돼 항상 최신이고, 서버 이벤트에서는 서버가 고정한 값 그대로 유지된다 — 양쪽 다 이미 정확한 값을 들고 있는데 화면만 별도로(그리고 다르게) 계산하고 있었을 뿐이다. **완료 기준** — 서버 이벤트의 제목·장소·일시를 바꿔도 표시된 URL이 그대로임(실제 slug와 계속 일치). 도메인이 실제 배포 주소와 일치. **실측**: 브라우저로 서버 연결된 이벤트(261003)를 열어 "생성될 URL"이 콘솔 카드의 실제 슬러그(`/meridian-arte-seoul-261003`)와 일치함을 확인 → 제목을 크게 바꿔도(실서버 PATCH까지 확인) URL 표시가 그대로임을 확인 → 원래 제목으로 되돌려 로컬 테스트 데이터 정리. `npm run lint`·`build` 통과 | `components/screens/EditorScreen.tsx`, [[log]] 2026-09-21 |
| FE-34 | 아젠다 편집이 "방금 저장됨"으로 표시되지만 서버엔 반영되지 않는다 | 완료. **무엇** — `EditorScreen.tsx`의 `AgendaSection`(드래그 순서 변경·키보드 이동·라이브러리에서 가져오기·삭제, 5곳)이 세션을 바꿀 때마다 `saved: '방금 저장됨'`/`'변경 저장 중…'`을 찍는데, `StudioProvider.tsx:252-254`가 세션(아젠다)만 바뀐 델타는 `detailPatchToBody`가 null을 돌려줘 **애초에 서버 저장을 예약하지 않는다**(주석에 이미 그렇게 적혀 있었다 — PUT .../sessions 계약이 FE-24·FE-25 몫으로 남아 있어서다, FE-30 결정). 즉 아젠다를 아무리 편집해도 "저장됨" 문구만 뜨고 실제로는 새로고침하면 사라진다. **외부 다각도 리뷰(프론트/플로우/PM/개발자/사용자 관점)가 지적**, 코드로 재확인 후 등록·수정까지 같은 세션에서 처리했다. **왜** — 사용자가 저장됐다고 믿고 새로고침하면 아젠다 편집을 통째로 잃는다. 이건 서버 쓰기 기능을 만드는 문제(FE-24·FE-25)가 아니라, **아직 없는 기능을 있는 것처럼 말하는** UI 문구의 문제라 훨씬 작고 급하게 고칠 수 있다. **완료 기준** — 아젠다 변경 시 문구가 "미리보기에만 반영됨"(드래그 중엔 "미리보기 반영 중…")으로 바뀌어, 저장이 아니라 로컬 미리보기 반영임이 편집 시점에 드러난다. 실제 필드 저장(기본 정보 등, 서버 이벤트에 한함)의 기존 "변경 저장 중…"→"방금 저장됨"/"저장 실패" 흐름은 그대로 둔다(그건 실제로 `PATCH`가 나가고 성공/실패로 갈린다 — 정직하다). **실측**: 브라우저로 서버에 연결된 이벤트를 열어 아젠다 항목 삭제 → 헤더 상태가 "미리보기에만 반영됨"으로 바뀌는 것 확인, 기본 정보 필드 수정은 기존과 동일하게 "변경 저장 중…"이 뜨는 것도 회귀 없음 확인. `npm run lint`·`build` 통과. **부수 발견(고치지 않음)**: 같은 검증 중 로컬 전용(서버 미연결) 목업 이벤트에서 기본 정보 필드를 수정하면 `saved`가 "변경 저장 중…"에서 영영 안 바뀌는 것을 발견했다 — `BasicSection`의 onChange가 서버 이벤트 여부와 무관하게 무조건 그 문구를 찍는데, 로컬 전용 이벤트는애초에 `flushServerSave`가 안 걸려 아무도 다음 상태로 안 옮겨준다. 아젠다와 증상은 다르지만 뿌리(로컬 전용 상태를 서버 저장 문구로 말한다)는 같다 — 범위 밖이라 이번엔 안 건드리고 과제로만 남긴다 | `components/screens/EditorScreen.tsx`, [[log]] 2026-09-21 |
| FE-13 | 디자인 시스템 문서화 + 공용 컴포넌트 라이브러리 | 완료. **다크모드 배지 갭(9절 알려진 갭, [[log]] 2026-08-19)을 이번에 닫았다** — 상태 배지(`pillStyle`)·리포트 샘플/증감 배지·에디터 대비비 통과/미달 배지가 각자 `공개`(hue 145)·`검수대기`(hue 78)·`미달`(hue 28) 계열 oklch 리터럴을 개별 정의하고 있었고 전부 다크 페어가 없었다 — `globals.css`에 `--tone-{success,warning,danger}-{bg,fg}` 3쌍을 다른 다크 페어(brand-soft 등)와 같은 변환 규칙(배경 L 0.95→0.28~0.3, 글자 L 0.4~0.5→0.76~0.8)으로 추가하고, `lib/ui.ts`의 `UI`에 노출했다. **`components/ui/`에 5개 컴포넌트 신설**: `Badge`(tone: success·warning·danger·muted·faint — `pillStyle` 대체), `Card`(surface+line+radius, padding/radius 조절 가능), `SectionTitle`(제목+설명+선택적 action), `EmptyState`, `TextInput`(라벨+입력+힌트). **적용 범위**: 콘솔의 상태 배지·빈 상태, 리포트의 샘플 데이터/증감 배지·카드 3곳, 에디터의 섹션 헤더 5곳·기본정보 입력 필드·URL 카드·인증서 미리보기 카드·테마 드래프트 카드·대비비 검증 카드+배지 4곳. **참가자 마이크로사이트는 손대지 않음**(범위 밖, 색 분리 원칙). 부수로 성공/실패 텍스트 색 리터럴(URL 중복 없음 안내, 각종 에러 메시지) 5곳도 같은 톤 토큰으로 교체 — 배지는 아니지만 같은 hue 145/28을 다크 페어 없이 쓰고 있어 같은 갭이었다. **실측**: 브라우저로 콘솔(라이트/다크)·리포트(라이트/다크)·에디터 기본정보·테마 섹션(다크)을 확인 — 다크모드에서 배지가 파스텔로 뜨던 문제가 사라지고 톤이 살아있는 색으로 렌더됨을 확인, 라이트모드는 배지 색이 그대로다 — **"픽셀 동일"은 정확하지 않아 머지 전에 정정했다**(콘솔 상태 배지 배경 L 0.96·0.965 → 0.955, 보관 배지 글자 0.66 → `--faint` 0.62로 통일되며 미세하게 변했다. 육안 차이 없음). **머지 전 대조에서 회귀 1건을 잡아 고쳤다** — 리포트 증감 배지가 `<Badge style={{padding}}>`만 넘겨 Badge 기본값이 적용되면서 `font-weight` 700→650, 높이 약 19px→22px로 변했다(서버 렌더 HTML을 두 브랜치에서 받아 확인). 같은 PR의 에디터 배지들은 `height:'auto'`를 이미 덮고 있어 이 한 곳만 빠진 것이었다. 빈 상태(`EmptyState`)도 검색으로 트리거해 확인. `npm run lint`·`build` 통과 | `components/ui/{Badge,Card,SectionTitle,EmptyState,TextInput}.tsx`, `app/globals.css`, `lib/ui.ts`, [[design]] 1-1·8절 (2026-09-21 [[log]]) |
| FE-31 | 추출한 브랜드 색을 참가자 화면이 실제로 쓰게 한다 | 완료. `app/[slug]/page.tsx`가 서버 응답의 `theme.preset`(`{id, label, h, c}`)을 버리고 `PRESETS.find(...)`으로 빌트인 5종에서만 찾던 것을 고쳤다 — `data.theme.preset`이 있으면(추출 프리셋) 그걸 그대로 `Preset` 타입으로 쓰고, 없을 때만(빌트인이거나 프리셋 없음) 기존 id 매칭 경로로 폴백한다(`data.theme.preset ?? PRESETS.find(...) ?? PRESETS[0]`) — 서버 응답의 `preset` 필드가 이미 `Preset` 인터페이스(`{id, label, h, c}`)와 정확히 같은 모양이라 변환 없이 바로 쓸 수 있었다. 낡은 주석("스튜디오가 CRUD API에 연결 안 돼 있어 이 경로로 실제 프리셋이 저장될 일이 없다")을 정리하고, `PublicEvent` 인터페이스에 `theme.preset` 필드를 추가했다. README「알고 있는 한계」의 해당 항목도 지웠다. **실측**(로컬 dev, 로컬 D1에 직접 INSERT — `POST /api/presets`는 로그인이 필요해 우회): `origin='extracted'` 프리셋(hue=20 진한 레드, 5개 빌트인과 뚜렷이 다른 색)을 만들어 데모 이벤트에 걸고 브라우저로 확인 — 참가자 페이지가 추출된 색 그대로 렌더되고 브랜드 라벨도 그 프리셋 이름으로 표시됨. 이벤트의 `preset_id`를 다시 NULL로 돌리면 빌트인 폴백("슬레이트 뉴트럴")으로 회귀 없이 렌더. 콘솔 에러 0건. `npm run lint`·`npm run build`(타입 검사 포함) 통과 | `app/[slug]/page.tsx`, `README.md` ([[log]] 2026-09-21) |
| FE-32 | `Microsite.openDocument`에 재진입 가드가 없다 — 다른 자료를 연달아 열면 낡은 응답이 화면을 덮어쓸 수 있음 | 완료. FE-29 PR #50 리뷰 중 발견돼 별도 등록한 과제. `openDocument`에 재진입을 막는 장치가 전혀 없어서(`loadingDocId` state뿐 — 렌더 이후에나 반영돼 같은 틱의 재호출을 못 막음), 문서 A를 연 직후 문서 B를 열면 두 fetch가 동시에 돌고 **나중에 도착하는 쪽이 무조건 이겼다.** **시행착오**: 첫 시도는 `activeDocIdRef`(문서 id를 값으로 저장)로 "지금 유효한 타깃"을 추적했는데, `/code-review`가 근본 결함을 찾았다 — **id로 판정하면 같은 문서를 A→B→A로 다시 열 때 낡은 A 요청과 새 A 요청을 구분 못 한다.** 낡은 쪽이 먼저 응답하면 그게 "최신"으로 오인돼 적용되고, 그 finally가 ref를 지운 뒤 진짜 최신 응답(2차 A)이 도착하면 오히려 그게 "낡음"으로 버려져 — 방금 성공한 요청이 조용히 사라지고 화면엔 가짜 에러만 남는 역전이 생길 수 있었다. **호출마다 고유한 단조 증가 토큰**(`requestIdRef`, 값이 아니라 매 호출을 구분하는 카운터)으로 다시 설계해 — 항상 "가장 마지막 호출의 토큰"만 유효하다고 판정하므로 A→B→A든 몇 번을 오가든 구조적으로 안전하다. **실측**(로컬 dev, 실제 이벤트에 자료를 만들어 검증 — 전부 실제 PDF 업로드): ① A를 열고 B를 열었는데 A가 더 늦게 응답 → 화면·`doc_view` 로그 둘 다 B만 반영, A는 로그도 안 남음. ② 같은 문서 3연타 → `hits` 정확히 +1만 증가. ③ **A→B→A 엣지케이스**(1차 A 요청만 3초 지연, B와 2차 A는 빠르게) → 두 A 요청이 모두 도착한 뒤에도 화면엔 A가 정상 표시되고 가짜 에러 메시지 없음(토큰 방식으로 재설계한 뒤에만 통과 — `activeDocIdRef` 버전으로는 이 케이스가 깨졌을 것). 정상 경로 회귀 없음, 콘솔 에러 0건. **리뷰가 함께 찾은 별개 문제(자원 낭비, 정확성 버그 아님)는 FE-33으로 분리** — `fetchWithTimeout`이 외부 abort signal을 받을 방법이 없어 버려진 요청이 자체 타임아웃(8초)까지 그냥 계속 돈다. 여러 기능이 공유하는 `lib/api.ts` 유틸이라 이 커밋에 안 넣음. `npm run lint`·`npm run build`(타입 검사 포함) 통과 | `components/Microsite.tsx` ([[log]] 2026-09-21) |
| FE-29 | 자료 카드를 네이티브 `<button>`으로 리팩터 | 완료. `role="button"`·수동 `tabIndex`·직접 구현한 Enter/Space `onKeyDown`을 걷어내고 `<button type="button" disabled={unavailable}>`로 교체했다 — 카드 룩은 `width: 100%`·`textAlign: 'left'`·`appearance: 'none'` 리셋으로 유지(전역 `button { font-family: inherit }`는 이미 있어 추가 리셋 불필요). **시행착오**: 첫 PR에서 `/code-review`가 "로딩 중(서명 URL 재요청) 연타하면 `openDocument`가 중복 호출된다"는 인접 결함을 지적해 `disabled={unavailable \|\| loading}`로 막았는데, **그 수정 자체가 새 회귀를 만들었다** — 포커스된 버튼이 `disabled`로 바뀌는 순간 브라우저가 강제로 blur시켜 키보드 사용자가 포커스를 잃고, 게다가 `loading` state는 렌더 뒤에나 반영돼 같은 틱의 연타는 애초에 못 막았다(두 번째 `/code-review` 발견). 또한 그 수정은 FE-29의 범위(버튼 전환)와 무관한 별개 결함이라 커밋·PR 컨벤션에도 어긋났다. **그래서 `disabled={unavailable}` 단독으로 되돌렸다** — 레이스 컨디션은 FE-32로 분리 등록(진짜 수정은 `loadingDocId` state가 아니라 함수 진입 시 동기적으로 갱신되는 ref로 해야 한다). 네이티브 `disabled`가 접근성 트리에서 완전히 빠지는 것(vs 기존 `aria-disabled`)과 일부 Chromium이 `disabled` 버튼의 `cursor` 인라인 스타일을 무시할 수 있는 것은 검토 후 감수하기로 했다 — 전자는 이 과제가 원래 의도한 표준 패턴 자체이고(WAI-ARIA는 완전 차단엔 네이티브 `disabled`를 권장), 실제 스크린리더 검증 도구가 없어 추측 대신 트레이드오프로 기록만 한다. 후자는 기능 영향 없는 화면버전별 코스메틱 이슈. **실측**: ready·pending 두 자료를 만들어(하나는 실제 PDF 업로드) 브라우저로 비교 — 비활성 카드는 클릭해도 무반응, `.focus()` 호출 자체가 거부됨(네이티브 disabled 확인), 정상 카드는 클릭·키보드 Enter 둘 다 정상 오픈. 콘솔 에러 0건, `npm run lint`·`npm run build` 통과 | `components/Microsite.tsx` ([[log]] 2026-09-20) |
| FE-27 | PdfViewer 리소스 정리 — 언마운트 시 렌더 취소 + 재오픈 캐싱 | **①·③은 고쳤고, ②는 재사용하지 않기로 했다.** ① 페이지 렌더 effect의 cleanup이 `cancelled = true`만 두고 실제 pdf.js 렌더 태스크는 취소하지 않아, 언마운트 뒤에도 진행 중이던 렌더가 이미 분리된 canvas에 계속 그려질 수 있었다 — cleanup에서 `renderTaskRef.current?.cancel()`을 호출하도록 추가. ② `PDFDocumentProxy`를 마운트 간(같은 문서를 닫았다 다시 열 때) 캐시할지 검토했으나 **안 하기로 했다** — 참가자 페이지의 `url`은 서명 URL(`lib/r2.ts` `signDocumentUrl`)이고 `exp`가 호출 시각의 초 단위 타임스탬프라 열 때마다(`Microsite.openDocument`가 "열기 직전에 새로 받는다") **사실상 거의 항상** 다른 문자열이 된다(같은 초 안에 재요청하면 우연히 같을 수 있어 URL 자체를 캐시 키로 신뢰할 수는 없다 — 문서 id·파일 버전을 키로 쓰는 대안도 검토는 했다). 정적 URL(스튜디오 미리보기의 `DEMO_DOCUMENTS`)만 히트하는 캐시를 위해 모듈 전역 캐시 + 수명 관리(참조 카운트, destroy 시점 조율)를 들이는 비용이 이 경로의 실제 무게에 비해 크다고 판단. **③ 리뷰가 찾은 추가 결함**: 문서 로드 effect가 `pdfjsLib.getDocument({ url }).promise`를 바로 await해서, **다운로드가 끝나기 전에 언마운트되면 cleanup이 취소할 대상을 못 찾았다**(`pdfRef.current`는 resolve된 뒤에만 채워짐) — `getDocument()`가 반환하는 loading task를 `loadingTaskRef`에 즉시 담아 cleanup에서 무조건 `destroy()`하도록 고쳤다. **실측**: ①은 페이지 이동을 빠르게 연달아 누른 뒤 바로 닫아도 콘솔에 미처리 예외 없음. ②는 같은 문서를 두 번 열어 `exp`·`sig`가 다른 별개의 서명 URL로 각각 다운로드됨을 확인. ③은 검증 방법을 한 번 틀렸다가 바로잡았다 — 처음엔 `/api/files` 라우트에 `AbortSignal`을 안 보는 순수 `setTimeout` 지연만 넣어서, 클라이언트가 취소해도 서버가 5초를 다 채우고 `200`을 찍는 **거짓 신호**를 봤다. 지연을 `request.signal`의 `abort` 이벤트로 감싸 재검증하니, 뷰어를 닫은 시점(~800ms)에 서버 로그가 `client aborted after 825ms`를 정확히 찍었다(닫지 않고 두면 `delay completed without abort` → 정상 렌더, 회귀 없음도 확인). 임시 계측 코드는 커밋 전 원복. `npm run lint`·`npm run build`(타입 검사 포함) 통과. **④ PR을 열어둔 채 `/code-review high` 재검증에서 리팩터 회귀 1건 추가 발견**: ③을 고치며 `getDocument()` 호출 자체가 `try` 밖으로 빠져, 동기적으로 던지면 `catch`가 못 잡아 `setState('error')`가 안 불리고 뷰어가 "불러오는 중…"에 멈출 수 있었다 — `getDocument()` 호출을 다시 `try` 안으로 옮겨 원래 안전성을 복구. 같은 리뷰의 "`RenderTask.cancel()` 중복 호출" 후보는 pdf.js 6.3.289 소스 추적으로 **기각**(모든 단계가 멱등 가드로 no-op). 브라우저로 재확인(UTF-8 파일로 문서명 보내 인코딩 재발 방지) 완료 | `components/PdfViewer.tsx` ([[log]] 2026-09-20) |
| BE-32 | 로그인 상태에서 다른 제공자를 잇는 명시적 흐름 | **기각(구현하지 않음).** 사용자 판단 — *"이메일이 달라지거나 다른 계정으로 로그인해야 되는 상황도 있어서 굳이 하나의 계정으로 안 묶어도 될 것 같다."* **제공자별 별개 계정을 채택된 모델로 확정한다.** 근거 넷: ① 제공자가 Google 하나뿐이라 **갈릴 일 자체가 아직 없다** — 존재하지 않는 문제였다. ② **연결을 안 하면 탈취 표면이 0이다** — 연결 기능은 "이 `sub`를 이 계정에 붙여도 되는가"라는 판정을 하나 더 만드는데, 그게 틀리면 곧 계정 탈취이고 그건 BE-26이 닫은 바로 그 구멍이다. ③ "이메일이 바뀌는 상황"은 **이미 처리돼 있다**(BE-26·BE-30 — 이메일은 표시용이고 겹쳐도 별개 계정이 생긴다). ④ 강제로 묶으면 제품이 "이 둘은 같은 사람"을 단정하는 것이라, 업무용/개인용을 일부러 나누는 경우를 방해한다. **감수하는 것**: Google 계정을 잃으면(해지·Workspace 회수) 그 계정의 이벤트에 못 들어간다 — 다만 이건 **연결이 아니라 「계정 복구」라는 다른 문제**이고(제공자를 둘 이어놔도 둘 다 잃으면 같다), 지금은 데모 이벤트가 `owner_id IS NULL`이라 게스트 경로가 열려 있고 운영자 데이터가 쌓이는 단계도 아니다. [[0011-account-link-key]]의 「감수하는 것」·「재검토 트리거」를 함께 개정했다 — **그냥 지우면 트리거가 죽은 링크로 남아, 나중에 "같은 사람이 갈리네"에서 이메일 매칭을 되살릴 위험**이 있다(그 유혹이 정확히 BE-26이 닫은 구멍이다) | [[0011-account-link-key]] 개정, [[log]] 2026-09-17 |
| BE-22 | 커스텀 도메인 + 엣지 rate limiting | **도메인은 완료, 엣지 규칙은 기각.** `sympo.superjacob.com`을 워커에 붙였다 — `superjacob.com`이 **이미 Cloudflare 네임서버를 쓰고 있어**(keira/keanu) 존이 존재했고 구매·이전·비용이 없었다. `wrangler.jsonc`의 `routes` + `custom_domain: true`. **`routes`를 쓰면 wrangler가 workers.dev 서브도메인을 끈다** — 모르고 배포해 기존 주소가 전 경로 404(`error code: 1042`)가 됐고 실측으로 잡아 `workers_dev: true`로 되살렸다(README·위키 9곳이 그 주소를 가리킨다). **엣지 규칙은 기각했다** — 무료 플랜 Rate Limiting Rule은 **카운팅 기준이 IP 단독**이라 걸면 [[0006-rate-limit-key]]가 기각한 "행사장 단일 IP에서 방 전체 잠김"을 엣지에서 되살린다. 산술도 안 맞는다: 참가자 100명 폴링이 한 IP 10초당 200~330 요청인데, 그걸 안 막는 임계값은 초당 100 요청을 통과시켜 **17분이면 Workers 일일 한도를 태운다**. Managed Challenge도 0006이 CAPTCHA를 기각한 이유(고령 사용자 마찰)가 그대로 적용돼 참가자 경로에는 안 건다. **얻은 것은 존 확보** — 플랜을 올리면 바로 규칙을 걸 수 있다. 무료 플랜 제약은 공식 문서로 재확인(0002의 지시) | [[0013-edge-rate-limiting-rejected]], `wrangler.jsonc` ([[log]] 2026-09-17) |
| BE-27 | 자료 status와 파일 존재를 일치시킨다 | 완료. **검사를 한 군데 더 넣는 대신 값의 출처를 없앴다** — 과제가 "후자가 근본적"이라 적은 파생 방식을 택했다. `status`는 이제 `PUT …/documents`의 **입력이 아니고**(`validateDocumentsBody`가 본문의 값을 읽지 않는다) 라우트가 `r2_key` 유무에서 파생시킨다(`UPDATE … SET status = CASE WHEN r2_key IS NULL THEN 'pending' ELSE 'ready' END`, 신규 INSERT는 항상 `'pending'`). **검사는 잊을 수 있지만 파생은 잊을 수 없다** — 예전 검사가 신규 생성에만 있었고(`id === null && status === 'ready'` → 400) 기존 항목 수정 경로에 없었던 것이 정확히 그 실패다. **이미 어긋나 있는 행은 코드 변경으로 안 고쳐지므로** 마이그레이션 `0015_document_status_from_file.sql`로 맞췄다(파생 규칙과 같은 식, 데이터는 지우지 않고 status만 조정). **실측**(로컬 dev + 로컬 D1): ① 파일 없는 기존 자료에 `status:'ready'`를 실어 PUT → 응답 `pending` ② 업로드 후 `ready`가 되는 기존 흐름 회귀 없음 ③ 그 뒤 `status:'pending'`을 실어 이름만 바꿔 저장 → **`ready` 유지 + 이름은 반영** ④ 새 자료에 `ready`를 실으면 400이 아니라 `pending`으로 생성 ⑤ 마이그레이션은 `r2_key` NULL + `ready`인 행을 일부러 만들어 적용 → `pending`으로 정정 확인. `lib/agenda.test.ts` 신설 4건. `npm run lint`·`test`(32건)·`build` 통과. API-Guide-FE에 "`status`는 보내도 무시되고 응답 값을 쓰라"를 명시 | `lib/agenda.ts`·`app/api/events/[id]/documents/route.ts`·`migrations/0015_document_status_from_file.sql` (2026-09-14 [[log]]) |
| BE-28 | 데모 리셋이 테마를 되돌리지 않는다 | 완료. **원인은 두 경로의 보이지 않는 비대칭이었다** — 데모 행이 없을 때 타는 INSERT는 테마 컬럼을 아예 안 써서 **스키마 DEFAULT가 채웠고**, 매일 자정의 정상 경로인 UPDATE는 SET 목록에서 빠져 있었다. 그래서 "새 DB에서는 맞고 운영 중에는 틀린" 상태였다. 고칠 때 UPDATE에 6컬럼을 더하는 데 그치지 않고 **`DEMO_THEME` 상수를 두어 INSERT·UPDATE가 같은 값을 보게** 했다 — 그 비대칭이 애초에 생기지 않게. **범위를 전수로 검증했다**: `events` 23컬럼을 UPDATE의 SET 목록과 대조해 안 되돌아오는 9개를 추렸고, 그중 `slug`(데모 행을 찾는 키)·`created_at`(행 생성 시각)은 되돌릴 대상이 아니며, **0013이 막 추가한 `client_ref`는 쓰기 경로가 `POST /api/events/import`의 신규 생성뿐이고 그마저 `owner_id = 나`와 함께 도는데 데모는 `owner_id IS NULL`이라 드리프트 경로가 없다**고 확인했다(과제 범위가 정확했다). `key_visual`을 NULL로 밀어도 R2 고아가 안 생긴다 — 키비주얼은 아직 R2로 가는 경로가 없다(FE-19). **검증**: 회귀 테스트 2건을 넣고 **수정을 되돌려 실제로 실패하는지 확인**했다(가짜 D1이 SQL 문자열만 본다는 한계를 감안해, 값 복귀는 따로 실측). 가짜 D1에 `demoExists: false`를 더해 **INSERT 분기도 검사 대상에 넣었다**. 실측은 코드가 발행하는 SQL과 바인딩을 그대로 뽑아 로컬 D1에 걸었다 — 테마를 `amber/dark/solid/컴팩트/외부URL/grid`로 오염시킨 뒤 리셋하니 6컬럼이 전부 초기값으로 복귀(바인딩 순서도 이 경로로 확인). `lint`·`test`(79건)·`build` 통과 | `lib/seed.ts`, [[log]] 2026-09-16 |
| BE-31 | 업로드 rate limit을 원자적 예약(admission)으로 | 완료. **착수를 측정으로 시작했다** — 같은 토큰으로 20MB 16개를 동시에 시작하니 **15건이 통과해 300MB가 들어갔다**(정책은 5분 200MB). 실제로 멈춘 것은 rate limit이 아니라 이벤트 총량 300MB였다. 판정과 증가를 **SQL 한 문장**으로 묶어(`reserveRateLimit`) R2 `put` 직전에 예약한다. 버킷마다 조건부 upsert를 따로 보내면 일부만 통과해 **보상 쓰기**가 생기는데, 그건 [[0008-rate-limit-counter]]가 *"무효 요청이 쓰기 티어를 태우면 Q&A·설문·로그가 전부 죽는다"*며 기각한 구조라 게이트를 SELECT의 WHERE 한 곳에 모았다 — 거짓이면 0행이라 **어떤 버킷도 오르지 않는다.** 로컬 D1 실측 4건: 거절 시 `changes=0`·값 불변 / 통과 시 `changes=4`·각 버킷 정확히 cost(자기 관찰 없음 — `195+5<=200` 경계를 일부러 만들어 갈랐다) / upsert에서 `RETURNING` 동작 / **거절 12건 뒤 카운터가 1도 안 움직임.** 물질화는 엔진 세부라 거기 기대지 않고 `RETURNING`으로 받은 버킷이 전부가 아니면 오른 것만 되돌린다. 되돌리는 기준은 **R2 `put`이 일어났는가** 하나다. 수정 후 재측정: 10건 통과·6건 429·카운터 정확히 200. 정상 현장 시나리오(4MB×5)는 안 막힘. `lint`·`test`(77건)·`build` 통과 | [[0012-upload-admission]], [[log]] 2026-09-16 |
| FE-30 | 스튜디오를 D1에 연결하는 경로 결정 (FE-15·19·23·24·25의 공통 차단 해제) | 완료. **id 지목 경로(ⓑ)를 채택** — 워크스페이스 경로(ⓐ, BE-15 의존)는 유예. `/events/[id]/edit`가 `GET /api/events/[id]`로 실제 D1 이벤트를 읽어 로컬 목업 배열에 병합하고, 기본 정보(제목·장소·일시·좌장·예상 인원·테마·참여 토글) 편집은 `PATCH /api/events/[id]`로 서버에도 반영한다(디바운스 700ms — 첫 구현은 키 입력마다 PATCH를 보내 12글자에 요청 12번이 나가는 것을 실측으로 발견해 수정). 아젠다·자료 쓰기는 범위 밖(FE-24·FE-25 몫). **착수 중 발견한 구조적 문제**: 원래 `urlEventId` 판정이 "목업 배열(id 0~14)에 있는가"를 요구해, 목업 범위 밖의 **실제 D1 전용 id(운영 중인 이벤트는 이미 15개를 넘음)**가 영영 fetch 대상이 되지 못했다 — 판정을 서버 fetch의 성공/404로 옮기고, 로딩 상태를 렌더 중 파생값으로 만들어(react-hooks/set-state-in-effect 회피) 목업 id는 즉시 렌더, 순수 D1 id는 "불러오는 중" 경유 후 렌더, 진짜 없는 id만 404가 되도록 일반화. 슬러그는 서버가 실제로 붙은 이벤트에서 로컬 재계산을 하지 않도록 가드(안 그러면 "공개 URL"이 실제 주소와 어긋나는 FE-23류 버그 재발). 브라우저로 event id=1(목업 범위 안)·id=17(목업 범위 밖) 둘 다 실측 — 편집 → 새로고침 후 유지 → 참가자 페이지(`/[slug]`)에 반영까지 확인 | [[0010-studio-d1-slug-designation]], `components/StudioProvider.tsx`, `lib/studio-api.ts` (2026-09-15 [[log]]) |
| FE-26 | 자료 뷰어 오버레이 접근성 — role="dialog" + 포커스 트랩 | 완료. `PdfViewer.tsx`의 전체화면 오버레이에 `role="dialog"`·`aria-modal="true"`·`aria-label={title}` 추가. 열리면 첫 컨트롤(닫기 버튼)로 포커스 이동, `keydown` 핸들러가 경계(첫/마지막 포커스 가능 버튼)에서만 방향을 되돌리는 방식으로 Tab 트랩(중간 tabbing은 기본 브라우저 동작에 맡김 — 오버레이 안 버튼들이 같은 컨테이너 형제라 문서 순서가 이미 맞음). 스튜디오 에디터의 라이브 프리뷰(데모 PDF)에서 `role="dialog"`·`aria-modal`·`aria-label` 확인, 포커스 자동 이동 확인, 첫 컨트롤 Shift+Tab → 마지막으로 순환·마지막 Tab → 첫 컨트롤로 순환을 `KeyboardEvent` 디스패치로 직접 확인, Esc 닫기 회귀 없음 확인. `npm run lint`·`test`(23건)·`build` 통과 | `components/PdfViewer.tsx` (2026-09-15 [[log]]) |
| BE-29 | 업로드 남용 통제 — R2 증폭 차단 | 완료. **상한 세 겹**으로 닫았다: ① 파일당 20MB(기존) ② **이벤트당 300MB**(`MAX_EVENT_BYTES`, 신규) ③ **MB 단위 rate limit**(`UPLOAD_RATE_POLICY`, 신규 — 5분 200MB·하루 1GB, IP 400MB·2GB). **한도의 단위를 요청 수가 아니라 MB로 잡은 것이 이 과제의 핵심 판단**이다 — 지키려는 자원이 D1 행이 아니라 R2의 저장량·Class A 연산이라, 요청 수로 세면 "1MB를 20번"과 "20MB를 20번"이 같은 비용으로 취급되는데 실제로는 20배 차이다. `lib/rate-limit.ts`의 판정 구조(2층 키·KST 경계·성공한 쓰기에만 증가)는 그대로 재사용하고 `scope`에 `'upload'`만 더했으며, cost의 의미는 정책이 정한다(`uploadCostMb()`). 키는 **`x-client-token` + IP 2층** — 데모 이벤트가 `owner_id IS NULL`이라 `assertCanEdit`를 누구든 통과하므로 **익명 업로드가 정상 경로**라 user_id를 쓸 수 없다(과제가 예상한 대로). 창을 60초가 아니라 **5분**으로 잡은 이유는 "연자가 늦어 자료를 행사 중에 연달아 올린다"가 정상 경로라서다([[field-experience]]). **②가 필요한 이유**: `MAX_DOCUMENTS`가 60이라 파일당 20MB만으로는 이벤트 하나가 1.2GB까지 가고, 이벤트 생성은 아직 무인증이라(BE-13) 개수 상한도 없어 **무료 R2 10GB를 이벤트 9개로 넘긴다.** 판정은 `SELECT COALESCE(SUM(size_bytes),0) … WHERE event_id = ? AND id <> ?` — **교체 대상 자료를 뺀다**(옛 객체가 지워져 그 몫이 돌아오므로, 빼지 않으면 큰 자료를 한 번 올린 순간 그 자료를 고칠 수 없게 된다). 자료 조회·사용량 합·rate 카운터를 **한 batch로 묶어 요청당 D1 왕복 1회**를 유지했고, 카운터 증가는 `UPDATE documents`와 같은 batch에 실었다(ADR 0008). 신고 크기(`content-length`)가 있으면 **버퍼에 담기 전에** 한 번 걸러낸다 — 이미 한도를 넘긴 상대에게 20MB를 더 받아 메모리에 올릴 이유가 없다(못 믿는 값이므로 실제 크기로 다시 판정). **회수 경로도 함께 고쳤다** — 착수 후 `documentKey`가 회차마다 nonce를 붙인다는 것을 확인했다. 즉 **덮어쓰기는 제자리 갱신이 아니라 새 객체 생성**이고 옛 객체 삭제는 실패해도 넘어가는 경로인데, `purgeOrphanDocuments`의 판정이 "죽은 이벤트의 프리픽스"라 **살아 있는 이벤트 안에 밀려난 객체는 영원히 회수되지 않았다.** 판정을 `documents.r2_key` 집합과의 대조로 넓혀 죽은 이벤트 경우까지 함께 덮고, **1시간 유예**를 뒀다(R2 put과 D1 커밋 사이에 Cron이 겹치면 방금 올린 자료가 지워진다 — 확률이 아니라 피해로 판단). **실측**(로컬 dev + 로컬 D1/R2): 정상 현장 시나리오(4MB × 10회 연속) **전부 201**, 같은 자료에 20MB 반복 덮어쓰기는 **정확히 200MB에서 429 + 사유 노출**("5분에 200MB까지 올릴 수 있습니다"), 290MB를 쓰는 이벤트에 20MB는 **400 + 현재 사용량 노출**·8MB는 201, 290MB짜리 자료 **자신을 20MB로 교체하는 것은 막히지 않음**. `lib/r2.test.ts`·`lib/retention.test.ts` 신설(11건). `npm run lint`·`test`(39건)·`build` 통과. README「알고 있는 한계」의 업로드 서술 갱신 — **무효 요청 플러드는 여전히 못 막는다**(엣지 층, BE-22). **코드리뷰 4건 반영**(`/code-review high` — 전부 실제 결함): ① **`windowSeconds: 300`이 무시되고 실제 창은 1분이었다** — `windowKeys()`가 분 단위 스탬프를 버킷 이름으로 쓰고 `windowSeconds`는 TTL에만 쓰였다(기존 정책 3종이 전부 60초라 우연히 맞았고, **다른 값을 처음 쓴 것이 BE-29였다**). 문구는 "5분에 200MB"인데 5배 느슨했다. 정책의 창 길이로 내림하게 고쳤다 — 60초 정책은 결과가 같아 동작 불변. **첫 실측이 이걸 못 잡은 것은 11번이 전부 같은 1분 안에 끝났기 때문**이고, `lib/r2.test.ts`도 분 단위를 박아 둬 결함을 고정하고 있었다. 재실측은 200MB를 채운 뒤 **70초 기다렸다가** 재시도해 여전히 429임을 확인. ② **읽기·판정·증가가 각자 `Date.now()`를 불러** 본문 읽기가 창 경계를 넘으면 판정이 0을 읽어 통과했다(하루 경계에서는 그날 사용량이 사라진다) — 요청 시작 시각을 고정해 셋에 넘긴다. ③ **`Content-Length`가 없으면 상한 셋이 전부 뒤로 밀려** 청크 전송으로 100MB를 아이솔레이트(128MB)에 밀어 넣을 수 있었다 — 이 라우트에서 헤더 필수(청크 40MB → 400 실측). ④ **이벤트 총량이 읽고-쓰기라 동시 업로드가 상한을 넘겼다** — `UPDATE`의 `WHERE`에 총량 술어를 넣어 실제 천장으로 만들었다(D1 술어 직접 실행: 295MB+20MB → `changes 0`, 5MB+20MB → `changes 1`). `lib/rate-limit.test.ts` 신설. **Codex 교차 리뷰 추가 반영**: ① **이벤트 갈아타기로 토큰 한도가 2배**였다(scope가 `upload:e{id}`) — `RatePolicy.tokenScope`를 더해 업로드만 전역으로 뒀다. ② 고아 정리 테스트의 가짜 버킷이 **cursor 페이징을 한 번도 안 태우고** 있어 페이지를 쪼개도록 고치고 2건 추가. ③ `changes === 0`일 때 카운터가 이미 올라간 것은 R2 `put`이 실제로 일어났으므로 그대로 두고 주석에 명시. ④ **rate 카운터의 TOCTOU는 남겼다** — 동시 요청이 같은 카운터를 읽고 전부 통과할 수 있다. 저장량은 이벤트당 300MB가 잡으므로 남는 피해는 R2 연산·대역폭이고, 가장 간단한 해법이 [[0008-rate-limit-counter]]가 기각한 "무효 요청이 쓰기 티어를 태우는" 구조라 판단이 필요해 **BE-31로 등록**했다. 최종 `npm run lint`·`test`(45건)·`build` 통과. **미실측**: 프로덕션 왕복 | `lib/r2.ts`·`lib/retention.ts`·`app/api/events/[id]/documents/[docId]/upload/route.ts`, [[0006-rate-limit-key]]·[[0008-rate-limit-counter]] (2026-09-14 [[log]]) |
| BE-30 | `users.email`의 UNIQUE를 떼고 계정 연결 흐름을 만든다 | **①②만 완료, ③④는 BE-32로 분리**(아래). **착수는 코드가 아니라 측정으로 시작했다** — 과제가 "안전한 경로를 먼저 찾아 로컬 D1에서 실측한 뒤 마이그레이션을 쓴다"고 요구했고, 실제로 셋을 쟀다: ㉮ `PRAGMA foreign_keys = OFF`는 **D1에서 먹지 않는다**(실행해도 값이 1) ㉯ `defer_foreign_keys = true`를 켜고 부모를 DROP하니 **자식 2행이 0행**이 됐다 — 검사는 미뤄도 **FK 액션은 막지 않는다**는 것을 스크래치 테이블로 재현 ㉰ **마이그레이션 파일 하나는 원자적**이다(앞 문장 성공 + 뒤 문장 실패 → 앞 문장까지 롤백). ㉰이 없었다면 이 작업을 하지 않았을 것이다 — 중간 실패가 **모든 사용자 이벤트를 `owner_id NULL`로 만들어 자정 Cron이 지우는** 경로였기 때문이다. **해법은 제약을 끄는 것이 아니라 "가리키는 손"을 먼저 놓게 하는 것**이다(`0014_users_email_not_identity.sql`): CASCADE는 부모를 실제로 가리키는 행만 지우므로, DROP 전에 NULL 허용 참조(`events.owner_id`·`brand_presets.owner_id`)는 값을 임시 테이블에 적고 NULL로 내리고, NOT NULL 참조(`oauth_accounts`·`auth_sessions`)는 행째 대피시킨 뒤 비운다. 그러면 DROP 시점에 `users`를 가리키는 행이 0이라 CASCADE가 0행을 지운다. 재작성 후 되돌린다. **부수로 BE-26의 구현 하나가 깨졌다** — 신규 계정 batch에서 링크의 `user_id`를 `(SELECT id FROM users WHERE email = ?)`로 집고 있었는데 그건 **email이 UNIQUE라는 전제** 위에 있었다. `last_insert_rowid()`로 바꿨고, BE-26이 "보장을 문서로 확인할 수 없다"며 피했던 그 값을 **이번에 로컬 D1로 직접 재서**(같은 batch에서 INSERT 뒤 방금 넣은 행의 id를 정확히 돌려준다) 해결했다. `AccountLinkConflict`와 `auth=email_conflict`는 **거절할 일이 없어져** 제거했다(④는 이렇게 닫혔다). **실측**: 사용자 2명·oauth 2·세션 2·이벤트 3(소유 2 + 데모 1)·아젠다 2·자료 1·질문 1·로그 1·프리셋 7(소유 1)을 넣고 마이그레이션 적용 → **모든 행 수가 완전히 동일**(차이는 쿼리 시간 줄뿐), `owner_id` 값과 프리셋 소유가 **id 수준까지 일치**, `PRAGMA foreign_key_check` 무결, 스키마에서 `email TEXT NOT NULL`(UNIQUE 없음) 확인, **같은 이메일로 두 번째 행 삽입 성공**. 재작성 뒤 dev 서버로 **기존 세션이 그대로 동작**(`/api/auth/me`가 A·B를 각각 돌려줌)하고 인가 경계·프리셋 범위가 유지되는 것까지 확인. **미실측**: 실제 Google 계정 두 개로 같은 주소를 만드는 왕복(프로바이더 쪽 사람 손 작업이 필요하다). `npm run lint`·`test`(54건)·`build` 통과. **③(제공자 연결 흐름)은 BE-32로 분리** — 두 번째 제공자의 OAuth 앱 등록이 사람 손 작업이라 만들어도 실측할 대상이 없다 | `migrations/0014_users_email_not_identity.sql`·`lib/auth.ts`, [[Decisions/0011-account-link-key]] (2026-09-14 [[log]]) |
| BE-15 | 게스트 워크스페이스 가져오기 엔드포인트 | 완료(**BE-13 위에 쌓았다**). `POST /api/events/import` — 이벤트 + 테마 + 아젠다를 묶어 받아 **이벤트마다 batch 한 번**으로 넣는다. ① **멱등성**: 마이그레이션 `0013`이 `events.client_ref` + **부분 UNIQUE 인덱스 `(owner_id, client_ref)`**를 만든다. 이미 있으면 `status:'exists'`로 기존 행을 돌려준다 — 끊긴 네트워크에서 다시 누르는 것은 정상 경로다. **소유자를 키에 넣은 것이 요점** — `client_ref`만으로 유일하면 남의 ref를 알아낸 사람이 남의 이벤트를 받아볼 수 있다. ② **원자성 단위는 이벤트 하나**: 전체를 한 트랜잭션으로 묶지 않는다("9개 성공했는데 10번째 때문에 처음부터"가 더 나쁘다). 건별 결과 배열을 돌려주고 화면이 실패분만 재시도한다. ③ **바뀐 것을 응답에 싣는다**: slug 충돌 시 `slugChanged`, 서버에 없는 프리셋이라 색을 비웠으면 `presetDropped`. 후자는 착수 중에 필요해진 것이다 — BE-25가 프리셋 생성에 로그인을 요구하게 되면서 **게스트가 로컬에서 만든 추출 프리셋은 서버에 없고**, 그대로 넣으면 FK 위반으로 이벤트 하나가 통째로 실패한다. 값을 비우고 사실을 알리는 쪽이 맞다. ④ **상한 셋**: 한 번에 20개, 이벤트당 아젠다 60개(BE-14의 `MAX_SESSIONS` 재사용), 계정당 총 20개(BE-13 ⑦). rate limit cost는 **이벤트 수**다 — 가져오기 한 번이 생성 N번과 같은 무게다. **왕복**: 멱등성 조회·slug 충돌 조회·프리셋 존재 확인·rate 카운터·소유 개수를 **한 batch로 묶고** 그 뒤로는 이벤트당 batch 1회(총 3 + N). slug는 메모리에서 푼다(`resolveSlug`), 후보만 `LIKE`로 훑어 테이블 전수 조회를 피한다. 세션의 `event_id`는 `(owner_id, client_ref)` 서브쿼리로 집는다(BE-26이 `users`에서 쓴 것과 같은 수법 — batch는 바인딩이 먼저 끝난다). **실측**(로컬 dev + 로컬 D1, 사용자 2명): 이벤트 3개(각 아젠다 6개·테마) 가져오기 → 전부 `owner_id`와 함께 저장, 아젠다 6개씩·테마 6컬럼 반영, slug 충돌 2건이 `slugChanged`로 보고됨 / **같은 요청 재전송 → 전부 `exists`, 총 개수 불변**(멱등성) / **B가 A의 `client_ref`로 요청해도 A의 이벤트에 닿지 못하고 B 소유로 별도 생성** / 없는 프리셋 → `presetDropped` + 생성 성공 / 한 번에 21개 → 400 / 계정 총량 초과 → 400(사유에 현재 개수와 요청 개수) / 비로그인 401. **미실측**: 중간 실패 주입 — 남은 실패 경로가 전부 사전 검사에 막혀 **결정적으로 주입할 방법을 찾지 못했다**(slug는 메모리 해석이라 충돌이 구조적으로 불가능하고, 프리셋은 미리 걸러내며, clientRef 중복은 검증에서 막힌다). 코드상 격리는 이벤트별 batch + `try/catch`로 보장되지만 **실측하지 못했음을 명시한다.** `lib/import.test.ts` 신설 7건. `npm run lint`·`test`(49건)·`build` 통과 | `lib/import.ts`·`app/api/events/import/route.ts`·`migrations/0013_events_client_ref.sql`, [[0007-sso-and-account-model]] §1-1 (2026-09-14 [[log]]) |
| BE-25 | 프리셋 쓰기 경로 보호 | 완료(**BE-13 위에 쌓았다** — 인가 헬퍼를 공유한다). `POST /api/presets`는 **인증 호출이 0건**이었고 쿼리가 조건 없는 `ON CONFLICT(id) DO UPDATE`라 **id만 알면 남이 만든 프리셋을, 0006이 심은 내장 5종까지 아무나 덮어썼다.** 프리셋 색은 참가자 화면에 그대로 렌더되므로 **덮어쓰기가 곧 남의 행사 페이지 색을 바꾸는 것**이었다. 셋을 잠갔다 — ① 로그인 필수(401) ② **내장 수정 금지**: `origin`을 본문에서 받지 않고 `'extracted'`로 고정(받아서 검사하는 대신 **받지 않는 쪽**을 골랐다 — BE-27과 같은 판단) ③ **남의 것 덮어쓰기 금지**: `DO UPDATE`에 `WHERE owner_id = 나`를 달아 조건이 어긋나면 갱신도 오류도 없이 아무 행도 안 돌아오고, 그걸로 409를 낸다. **소유자 컬럼을 추가했다**(마이그레이션 `0012_preset_owner.sql`) — 과제가 "컬럼을 추가할지부터 정해야 한다"고 남긴 판단이다. `NULL = 주인 없음 = 내장(공용)`이라 **컬럼 하나가 두 가지를 동시에 정한다**(누가 고치나 / 누구에게 보이나), `events.owner_id`가 데모를 가르는 방식과 같은 규칙이라 새 개념이 없다. 기존 행은 전부 NULL로 남아 **아무도 덮어쓸 수 없다**(잠기는 쪽이 안전한 기본값). 목록도 `공용 + 내 것`으로 갈랐고 캐시를 `public`→`private`로 내렸다(보는 사람에 따라 달라지는 응답을 공유 캐시에 올리면 남의 목록이 간다). **409로 존재를 알려주는 것이 여기서는 맞다** — 다른 운영자 경로는 404로 존재를 숨기지만 프리셋 id는 라벨에서 파생되는 **공유 이름공간**이라, 충돌을 숨기면 FE가 다른 id를 고를 수 없다(아이디 중복과 같은 성격). 계정별 이름공간은 PK 변경=테이블 재작성이라 범위 밖. **실측**(로컬 dev + 로컬 D1, 사용자 2명): 비로그인 401 / 내장 `aurora` 덮어쓰기 409 + **원래 값 유지** / 본문에 `origin:'builtin'`을 실어도 `extracted`로 저장 / B가 A의 프리셋 덮어쓰기 409, A 본인 갱신은 성공 / 목록이 게스트·A·B에서 각각 `공용`·`공용+a-brand`·`공용` / **참가자 화면 색 렌더 회귀 없음** — A 소유 프리셋을 건 이벤트의 공개 응답이 무인증으로 `theme.preset={h:215,c:0.13}`을 그대로 싣는다(공개 조인은 소유권으로 거르지 않는다 — 참가자가 색을 못 받으면 화면이 깨진다). `lib/presets.test.ts` 신설 4건. `npm run lint`·`test`(46건)·`build` 통과 | `lib/presets.ts`·`app/api/presets/route.ts`·`migrations/0012_preset_owner.sql` (2026-09-14 [[log]]) |
| BE-13 · BE-24 | 인가 경계를 운영자 API에 적용 + 무인증 읽기 차단 | 완료(둘을 한 번에 — 같은 파일·같은 판정이라 나눠 하면 중간 상태에서 한쪽만 잠긴다). **쓰기**: `POST /api/events`가 로그인 필수(401)가 됐고 `owner_id`가 항상 채워진다 — 예전엔 비로그인 생성이 `owner_id NULL`, 즉 **데모와 같은 무소유 행**이라 만든 사람은 자정 리셋에 잃고 그때까지 **아무나 고치고 지울 수 있었다.** `PATCH`/`DELETE`와 하위 리소스는 이미 `assertCanEdit`를 지나고 있었다. **읽기**: 목록은 `owner_id IS NULL OR owner_id = <세션 사용자>`로 갈라 로그인 시 "내 것 + 데모", 게스트는 데모만 본다. 상세와 `/ops`는 **인증 호출이 0건**이었는데 소유자만 보도록 잠갔고 남의 것은 403이 아니라 **404**다. **데모를 로그인 사용자에게도 남긴 이유**: 데모는 숨길 남의 데이터가 아니라 제품이 스스로를 보여주는 화면이고, 로그인했다고 사라지면 "로그인하면 잃는다"가 하나 더 생긴다(ADR 0007). **`/ops`를 갈랐다**: 참가자 공개 리포트(`/[slug]/report`)가 같은 경로를 쓰고 있어 **하나만 잠그면 그 화면이 죽고 열어 두면 남의 지표가 샜다.** 관객이 다르므로 경로를 나눴다 — 운영자용은 소유자 전용(`private` 캐시), 참가자용은 새 `GET /api/public/[slug]/report`로 **공개 상태 기준**(`public` 캐시, 집계 수치만 — 개별 방문자·질문 본문·설문 응답은 안 실린다). 집계 계산은 `lib/ops.ts` **한 벌**로 모아 두 라우트가 공유한다(`attendanceRate`의 상한 1은 BE-19가 한 번 놓쳤던 값이다). `lib/api.ts`에 `fetchPublicReport`를 더하고 FE 리포트 페이지를 옮겼다. **왕복을 늘리지 않았다**(BE-13 ⑥): 세션을 따로 조회하는 대신 `sessionUserIdSql()` 서브쿼리를 대상 조회와 **같은 문**에 넣어 읽기 3종 전부 batch 1회를 유지했다. **⑦ 상한**: 생성 경로에 `EVENT_WRITE_RATE_POLICY`(user_id 키, 60초 15건·하루 60건)와 **계정당 20개** 총량 상한. 속도와 총량은 다른 것을 막는다 — 하루 한도씩 꾸준히 만들면 총량은 계속 는다. 20은 R2 근거다(BE-29의 이벤트당 300MB × 무료 10GB ≈ 전체 33개분). 카운터 증가는 INSERT와 같은 batch(ADR 0008). **착수 중 잡은 자기 결함**: 목록 쿼리의 `?1`이 하드코딩이라 소유권 조건을 앞에 붙이는 순간 **검색어가 아니라 사용자 id로 LIKE 검색**을 하게 됐다 — 자리번호를 계산식으로 바꾸고 회귀를 실측했다. **실측**(로컬 dev + 로컬 D1, 사용자 2명 세션을 직접 심어): 비로그인 POST 401 / B가 A의 상세·`/ops`·PATCH·DELETE 전부 404 / 게스트·A·B 목록이 각각 `[데모]`·`[데모, A의 행사]`·`[데모]` / 검색 회귀 통과 / **참가자 경로 전부 무인증 유지**(공개 조회 200, 공개 리포트 200, Q&A GET 200·POST 201, 로그 201) / 연속 생성 22회에서 **15번째부터 429 + 사유**, rate 창을 비운 뒤 이어서 만들어 **21번째부터 400 + 사유**(계정당 20개). 테스트 데이터는 정리했다. `lib/auth.test.ts`·`lib/ops.test.ts`에 12건 추가. `npm run lint`·`test`(42건)·`build` 통과 | `lib/auth.ts`·`lib/ops.ts`·`lib/events.ts`·`app/api/events/**`·`app/api/public/[slug]/report/route.ts` (2026-09-14 [[log]]) |
| BE-26 | 계정 연결을 이메일에서 끊는다 + 인증 콜백 잔여 3건 | 완료. ① **연결 키를 `oauth_accounts(provider, provider_account_id)` 하나로** — `SELECT id FROM users WHERE email = ?`로 같은 이메일 계정에 제공자를 잇던 경로를 없앴다(조용한 계정 탈취). 같은 이메일의 행이 있으면 잇지 않고 `AccountLinkConflict`(409)로 거절하고 콜백이 `/console?auth=email_conflict`로 접는다. **별개 계정 생성으로 가지 않은 이유**: `users.email`이 `NOT NULL UNIQUE`라 제약을 떼려면 테이블 재작성이 필요한데, `users`는 CASCADE 자식 셋의 부모이고 SQLite는 부모 DROP 시 암묵적 `DELETE FROM`을 돌리며 **CASCADE 액션은 비활성화되지 않는다** — D1은 `PRAGMA foreign_keys=OFF`를 안 주고 `defer_foreign_keys`는 검사만 미룬다. 값을 잃을 수 있는 마이그레이션을 보안 수정과 같은 커밋에 싣지 않고 **BE-30**으로 분리([[Decisions/0011-account-link-key]]). 부수로 로그인마다 이메일을 갱신한다(예전엔 최초 가입 시점 값이 굳어 있었다 — 굳은 값으로 계정을 잇고 있었다는 뜻). **갱신은 충돌하지 않을 때만** — 새 주소를 다른 행이 이미 쓰고 있으면 옛 주소를 유지한다(`SET email = CASE WHEN EXISTS(…자기 행 제외…) THEN email ELSE ? END`, 왕복 안 늘림). 그냥 덮어썼다면 표시용 값 하나 때문에 이미 링크된 사람의 로그인이 UNIQUE 위반으로 실패했을 것이다. ② **신규 생성을 `batch` 하나로** — `SELECT`→`INSERT users`→`INSERT oauth_accounts`가 따로 돌아 중간 실패 시 **oauth 링크 없는 고아 `users` 행**이 남았고, 그 행이 다음 로그인에서 다시 ①의 이메일 매칭을 불렀다(비원자성이 ①을 스스로 재생산). batch는 바인딩이 먼저 끝나 첫 문의 `RETURNING id`를 둘째 문에 못 넣으므로 둘째 문이 `VALUES ((SELECT id FROM users WHERE email = ?), …)`로 방금 넣은 행을 집는다 — `last_insert_rowid()`는 D1 batch가 문 사이에 보존한다는 보장을 문서로 확인할 수 없어 쓰지 않았다. ③ **콜백 `catch`가 `ApiError`만 접던 것**을 전부 접도록(로그는 `console.error`로 남기고 응답엔 안 실림) — 그 밖의 예외가 500 스택 페이지로 새고 있었다. ④ **`?error=` 분기를 state 대조 뒤로** — `fail()`이 state·redirect 쿠키를 만료시키는데 이 분기가 대조보다 먼저 돌아, 공격자가 피해자 브라우저를 `/api/auth/callback/google?error=x`로 보내기만 하면(이미지 태그 한 줄) **진행 중이던 정상 로그인이 'state 쿠키 없음'으로 실패**했다. `matchOAuthState()`를 신설해 판정을 맨 앞으로 올리고, 통과 전 실패는 쿠키를 건드리지 않는 `ignore()`로 분리. **실측**: 로컬 D1에서 batch 2문 실행 — 서브쿼리가 방금 삽입한 id로 해석되고, 둘째 문이 UNIQUE로 실패하면 **첫 문까지 롤백돼 고아 행이 0건**(`SELECT … WHERE email LIKE 'be26-%'`로 확인). `lib/auth.test.ts` 신설(7건) — 이메일 연결 금지·거절 시 INSERT 0건·두 INSERT가 같은 batch·`matchOAuthState` 경계. `npm run lint`·`test`(28건)·`build` 통과. **미실측**: 실제 Google 왕복(취소·`?error=` 주입·이메일 충돌 로그인)은 배포 후 확인이 남았다 | [[Decisions/0011-account-link-key]], `lib/auth.ts`·`app/api/auth/callback/google/route.ts`·`lib/auth.test.ts` (2026-09-14 [[log]]) |
| FE-20 | 참가자 화면에서 이벤트 로그 적재 | 완료. `lib/api.ts`의 `sendEventLogs()`(배치·실패 시 무시)로 4종을 전부 연결 — `page_view`(`app/[slug]/page.tsx` 로드 시 1회), `session_view`(`Microsite`가 아젠다 `<li>`를 `IntersectionObserver`로 감지해 1.5초 디바운스 배치, 같은 세션 중복 방지), `survey_complete`(FE-4 종료와 함께, 2026-09-09), `doc_view`(FE-6의 PDF 뷰어가 트리거를 만들어 준 2026-09-14에 마지막으로 연결). **트리거가 없어 보류했던 두 종을 각각 그 화면이 생긴 시점에 이어 붙이는 방식**으로 닫았다. 오프라인 유실은 FE-21이 FE-9의 재조회 경로에 얹어 해결 | `lib/api.ts`·`app/[slug]/page.tsx`·`components/Microsite.tsx`, PR #20·#22·#34·#39 ([[log]] 2026-09-06·09-09·09-14) |
| BE-12 | 인증 기반 — Google SSO + D1 세션 + 이벤트 소유권 컬럼 | 완료. 마이그레이션 `0010_auth.sql`(`users`·`oauth_accounts`·`auth_sessions` + `events.owner_id`), `lib/auth.ts`(PKCE·세션·업서트·만료 정리), 라우트 4개(`/api/auth/{google,callback/google,logout,me}`), 시드 삭제를 `owner_id IS NULL`로 한정, 만료 세션 정리를 자정 Cron에 추가. **2026-09-07에 작성됐으나 PR도 로그도 없이 33 커밋 동안 묵어** 2026-09-11에 되살렸고, 그 사이 0008·0009가 먼저 적용돼 마이그레이션 번호를 0007→0010으로 옮겼다. 코드리뷰·Codex 교차 리뷰가 **오픈 리다이렉터(백슬래시 변형)·쿠키 속성 주입·시드 id 충돌·자식 라우트 우회**를 잡아 핫픽스까지 완료. 실제 Google 로그인 왕복 실측(한글 이름 보존, `auth_sessions.id`가 SHA-256 64자라 원문 토큰 미저장, httpOnly 동작 확인). 인가 경계 확대는 BE-13 | [[0007-sso-and-account-model]], PR #37·#38 ([[log]] 2026-09-11) |
| BE-23 | 이벤트 상태 검증 + 상태 모델 재정의 | 완료. **범위가 착수 중에 넓어졌다** — 원래는 "허용값 검증 + 자동 전환 결정"이었는데, 사용자가 "상태를 구현하는데 재정의를 해야될것 같아"라고 지적해 전수 확인한 결과 **6종이 서로 다른 세 축을 한 칸에 담고 있었다**(발행 여부=사람 / 행사 시점=달력 / 보관=사람). 가운데 축(`공개예정`·`진행중`·`완료`)은 차이가 오직 `event_date`와 오늘의 관계라 사람이 고를 것이 없었고, 그래서 "누가 언제 바꾸나"(FE-24)와 "자동 전환을 어떻게"(BE-23 ②)가 같은 곳에서 막혀 있었다. **발행 상태 4종(`초안`·`검수대기`·`공개`·`보관`)만 저장하고 시점 3종(`예정`·`당일`·`종료`)은 `event_date`에서 파생**하는 것으로 재정의([[0009-event-status-axes]]). 이렇게 하니 ②가 **사라졌다** — 저장하지 않으면 전환할 값이 없어 Cron도 수동/자동 우선순위 규칙도 불필요하다. 처음에 파생을 낮게 봤던 이유("목록 필터·인덱스가 죽는다")는 **축을 가르지 않은 채 생각했기 때문**으로, 발행 상태는 여전히 저장값이라 `WHERE status = ?`가 살고 시점은 `event_date` 범위라 기존 `idx_events_status(status, event_date)`가 정확히 그 조합을 덮는다. **구현**: `lib/status.ts`(신규, 의존 0) — 4종·파생 `eventPhase()`·KST 기준 `todayKst()`·`isEventStatus()`를 한 정본으로 모으고 `lib/db.ts`(`PUBLIC_STATUSES` 재export)·`lib/data.ts`(`STATUS`)가 여기서 가져간다. 마이그레이션 `0009`가 `공개예정|진행중|완료`를 `공개`로 접는다(셋 다 이미 `PUBLIC_STATUSES`라 **공개 여부는 불변**). ①(검증)은 **PATCH만이 아니라 POST·목록 필터까지 세 경로 전부** — 착수 후 `POST /api/events`도 아무 문자열이나 받아 201을 돌려주던 것을 발견했다. `toEventDTO`가 파생 `phase`를 응답에 실어 보낸다(계산을 서버·클라이언트 양쪽에 두면 타임존 처리가 갈라진다). FE 정합도 같은 커밋에 포함 — 배지를 두 축으로 나눠 그리고(`phasePillStyle` 신설), 일괄 액션에서 시점 값을 빼고, 시드 데모의 `2026-08-15 + 진행중` 모순과 콘솔 목업의 고정 과거 날짜를 함께 고쳤다(목업은 오늘 기준으로 흩뿌려 `예정`·`당일`·`종료`가 한 화면에 나온다). `lib/status.test.ts`(신규, 8건) — 옛 시점 값이 되살아나지 않는지, KST 경계가 UTC로 새지 않는지를 고정. **실측**: 옛 값 3종 400 / 새 4종 200 / 목록 필터 400·200 / `보관` → 공개 페이지 404 / `phase` 응답 확인, 콘솔 브라우저 확인. `npm run lint`·`test`(20건)·`build` 통과 | [[0009-event-status-axes]], `lib/status.ts`, `migrations/0009_event_status_axes.sql` (2026-09-11 [[log]]) |
| 1 | 새 리포지토리로 이전 — 히스토리에 남은 원본 데이터 제거 | 완료. 기존 public 리포 삭제 후 동일 이름으로 재생성, 단일 초기 커밋(`cd1039c`)으로 시작 | 원격 clone 후 `git log -S` 조회 0건 (2026-08-15 [[log]]) |
| 2 | README 재작성 — 포트폴리오의 본체 | 완료. 문제 정의·설계 판단·알고 있는 한계 구성, 리포트 샘플 표기를 README와 UI 배지 양쪽에 명시 | `README.md`, `ReportScreen.tsx` (2026-08-15 [[log]]) |
| 3 | 정적 export + 무료 영구 배포 | **폐기.** 정적 전용으로는 Q&A·설문이라는 제품 핵심을 증명할 수 없어 백엔드를 붙이는 방향으로 대체. 배포 자체는 BE-2로 계승 | [[0001-backend-for-working-demo]] |
| FE-10 | 스튜디오 셸 브랜드 아이덴티티 적용 | 완료. OKLCH 205도 틸 `brand`/`brandPress`/`brandSoft` 토큰 + 로고 마크, 선택 상태·프라이머리 액션에만 적용. 마이크로사이트 틸 미혼입은 computed style 전수 스캔으로 검증(08-18 [[log]]) | [PR #5](https://github.com/fomula91/sympo-studio/pull/5)(머지), `lib/ui.ts`·`components/Logo.tsx` (2026-08-17~18 [[log]]) |
| FE-11 | 다크 인트로/소개 페이지 | 완료. `/intro` 라우트, 정정된 문제 정의 기반 문구, 실화면 스크린샷 4장. 이후 라이트 기본 + 다크 토글로 통합 전환 | [PR #6](https://github.com/fomula91/sympo-studio/pull/6)(머지 2026-08-23) (2026-08-17~18 [[log]]) |
| FE-12 | 소개 페이지 반응형 + 랜딩 재설계 | 완료. `auto-fit`+`minmax(min(Npx,100%),1fr)` 패턴으로 미디어쿼리 없이 반응형(375px 가로 오버플로 원인 실측·해결), 설계 판단 표→Before/After 카드, 결론 인용구 격상. 375/820/1440px 스크린샷 3종, 가로 오버플로 0 | PR #6에 포함(머지), [[design]] 6·7절 (2026-08-18 [[log]]) |
| BE-1 | D1 스키마 설계 + 이벤트 CRUD | 완료. 7개 테이블 + 이벤트 CRUD 4개 엔드포인트. 로컬 D1으로 생성·조회·수정·삭제 왕복, slug 충돌 회피, CASCADE 삭제, 설문 중복 방지 UNIQUE 확인. **서로 다른 이벤트가 각자의 아젠다·테마를 반환**하는 것까지 실측 | [[0005-d1-schema]], `migrations/0001_init.sql` (2026-08-15 [[log]]) |
| BE-6 | R2 자료 저장 + 업로드 + 서명 URL | 완료. 버킷 `sympo-studio-docs` + 바인딩 `DOCS`. `PUT …/documents/[docId]/upload`(PDF만·20MB·기존 행에 파일을 붙일 뿐 새 행을 안 만듦·교체 시 옛 객체 삭제), `GET /api/files/[...key]?exp&sig`(HMAC 서명+만료, 실패는 전부 404, `private` 캐시). **BE-7 공개 응답에 서명 URL을 실어** 참가자가 별도 발급 없이 열게 하고 `r2_key`는 경계 밖 유지. 서버는 PDF를 열어 보지 않는다(ADR 0002). **자정 Cron에 고아 R2 객체 정리 추가** — D1 CASCADE가 R2를 안 따라오므로 이것이 저장량의 유일한 상한. **결제 수단 등록에 따라 안전장치가 알람+코드 상한으로 바뀐 것을 [[Context]]에 기록** | PR #18(머지), [[API-Guide-FE]] (2026-09-02 [[log]]) |
| FE-1 | 이벤트별 편집 상태 분리 | 완료. `StudioState`에 평평했던 편집 필드(title/venue/date/host/cap/engage/presetId/mode/iconSet/density/keyVisual/kvPattern/sessions)를 `EventDetail`로 묶어 `EventItem`이 갖도록 이동, `editingId` + `patchEvent`로 "현재 편집 중인 이벤트만" 갱신. 콘솔 카드 클릭이 `editingId`만 바꾸고, "새 이벤트"가 실제로 `events` 배열에 새 항목을 만듦. 헤드리스 브라우저로 이벤트1 아젠다·테마·제목 편집 후 이벤트2가 영향받지 않는 것, 새 이벤트 생성이 독립 상태로 시작하는 것 실측. lint·build 통과, 콘솔 에러 0건 | `lib/types.ts`, `lib/data.ts`, `components/StudioApp.tsx`, `components/screens/{Console,Editor,Viewer,Report}Screen.tsx` (2026-08-24 [[log]]) |
| FE-2 | 라우트 분리 + 폰트 자체 호스팅 | 완료. 사용자와 상의해 범위를 좁힘 — `/[slug]` 공개 페이지는 만들지 않음(BE에 slug 조회 API 없음 + "행사 페이지는 관리자 화면 우측 미리보기로만" 결정). `/console`·`/events/[id]/edit`·`/report` 3개만 실제 라우트로 분리(`app/(studio)/` 라우트 그룹), 뷰어는 라우트 없이 `viewerOpen` 플래그로 현재 화면 위에 오버레이. 상태는 `StudioApp.tsx`(삭제)에서 `StudioProvider`(Context)로 끌어올려 레이아웃과 3개 페이지가 공유. Pretendard는 npm에서 가변 폰트 파일 1개(2.1MB)만 뽑아 `public/fonts/`에 커밋하고 `next/font/local`로 서빙 — 패키지 자체(72MB)는 `--no-save`로 설치해 `package.json`에 순증 없음. 헤드리스 브라우저로 3개 라우트 딥링크·카드클릭 네비게이션·뷰어 오버레이(URL 불변)·새 이벤트 생성 확인, 외부 요청 0건·콘솔 에러 0건 실측. lint·build 통과 | `lib/font.ts`, `components/Studio{Provider,Shell}.tsx`, `app/(studio)/**`, `app/{page,layout}.tsx` (2026-08-24 [[log]]) |
| FE-7 | 대비비 저장 게이트 실장 | 완료. `lib/theme.ts`의 근사식(`lum(L) = L³`, 채도·색상 무시)을 `culori`의 `wcagContrast`로 교체 — `contrastRows`가 이제 `theme.L.*`(명도 숫자) 대신 실제 `oklch(...)` 색 문자열을 비교한다. 신규 export `contrastAllPass(preset, mode)`로 게이트 판정을 한 곳에 모으고, `StudioShell.tsx`의 "공개하기" 버튼·내비 "뷰어" 아이콘 둘 다 이걸로 가드(미달 시 버튼 비활성 + "대비비 미달로 공개할 수 없음" 문구). 실측: 기존 내장 프리셋 5개는 근사·실측 양쪽 다 여유 있게 통과(가장 타이트해도 5.2:1, 기준 4.5:1) — 게이트를 걸어도 지금 데모는 안 깨짐. 이 저장소 첫 자동 테스트로 Vitest 도입(`npm run test`), `lib/theme.test.ts`에 5프리셋×라이트/다크 통과 회귀 + 인위적 고채도(`c=0.8`) 조합으로 실패 케이스(브랜드 위 텍스트 4.48:1, 기준 4.5:1 근소 미달) 검증 — 근사식이었다면 못 잡았을 미세 미달을 실제 sRGB 변환이라야 잡아낸다는 것의 증거. 헤드리스 브라우저로 UI 경로도 실측: 임시로 그 실패 프리셋을 `PRESETS`에 추가해 테마 탭에서 직접 선택 → 공개하기 비활성+사유 노출+강제 클릭해도 뷰어 안 열림, 정상 프리셋으로 되돌리면 재활성화까지 확인 후 원복(최종 diff엔 남지 않음). culori는 런타임 의존성(2.1kB, `@types/culori` devDependency 동반), lint·build·test 전부 통과, 콘솔 에러 0건 | `lib/theme.ts`, `lib/theme.test.ts`, `components/StudioShell.tsx`, `components/screens/EditorScreen.tsx` (2026-08-24 [[log]]) |
| FE-8 | 브랜드 컬러를 대표 이미지에서 추출 + 프리셋 축적 | 완료. `lib/colorExtract.ts` 신설 — 이미지를 48×48 캔버스로 축소해 픽셀을 채널당 8단계로 양자화한 히스토그램에 누적하고 무채색(채도 12% 미만) 버킷은 제외한 뒤 가장 큰 버킷을 `culori.oklch()`로 변환해 `{h, c}`를 뽑는다(로고·제품 이미지는 흰 배경이 대부분이라 단순 평균은 배경에 묻힘 — 무채색 제외가 핵심). `Preset` 타입을 `theme.ts`→`types.ts`로 옮겨 `StudioState.customPresets`(세션 동안 쌓이는 추출 프리셋, 이벤트가 아니라 앱 전체 공유)에 담고, `StudioProvider`가 `presets = [...PRESETS, ...customPresets]`를 컨텍스트로 노출해 4개 파일에 흩어져 있던 `PRESETS.find(...)` 반복을 단일 소스로 정리. `ThemeSection`의 "01 · 브랜드 프리셋" 그리드 끝에 "+ 이미지에서 추출" 타일 추가 — 추출 성공 시 스와치·라벨 입력·채도 슬라이더·실시간 대비비 배지가 뜨는 미리보기 패널이 열리고, `contrastAllPass`(FE-7)가 참일 때만 "프리셋으로 저장" 활성화(미달 시 "채도를 낮추면 통과할 수 있습니다" 안내 + 슬라이더로 채도를 낮춰 재시도). 저장하면 라벨을 슬러그화(중복 시 `-2` 접미사, BE-1의 slug 중복 처리와 같은 패턴)해 새 프리셋을 만들고 현재 이벤트에 즉시 적용. **실측으로 발견한 것** — 실제 sRGB에서 나올 수 있는 최고 채도급 색(마젠타 c≈0.32, 사이언·빨강·연두 등)을 전부 `derive()` 파이프라인에 넣어봐도 대비비 기준을 여유 있게 통과(최소 5.2:1) — `derive()`의 L-계층 차이가 넉넉해서, FE-7에서 만든 슬라이더/미달 UI는 실제 사진으로는 사실상 트리거되지 않는다(FE-7 테스트가 썼던 실패 케이스는 sRGB 밖의 인위적 채도였음). 그래도 게이트·수동 조정 경로 자체는 FE-7과 같은 검증된 함수를 그대로 재사용하므로 구조적으로는 정상 동작 — 완료 기준이 요구하는 "경로가 있음"은 충족. 헤드리스 브라우저로 실제 캔버스 이미지를 만들어 업로드 → 미리보기·대비비 배지 확인 → 저장 → 그리드에 새 타일 반영 → **SPA 네비게이션으로 다른 이벤트를 열어도 프리셋이 남아있고 라이브 프리뷰에 즉시 반영**되는 것까지 실측. lint·build·test 전부 통과, 콘솔 에러 0건 | `lib/colorExtract.ts`, `lib/types.ts`, `lib/theme.ts`, `components/StudioProvider.tsx`, `components/screens/{Editor,Viewer}Screen.tsx`, `components/StudioShell.tsx`, `app/(studio)/events/[id]/edit/page.tsx` (2026-08-24 [[log]]) |
| FE-9 | 네트워크 단절 대응 | 완료(범위 축소 — 사용자와 상의 후 결정). 착수 전 확인: 이 코드베이스엔 `fetch`/폴링이 **전혀 없다**(`Microsite.tsx`의 아젠다·자료는 순수 로컬 state, Q&A·설문 버튼은 `onClick`도 없는 비활성 placeholder) — FE-3(Q&A 연결)·FE-5(리포트 실측)가 아직 열린 과제라 "폴링이 끊기는 상황" 자체가 지금은 존재하지 않는다. 그래서 사용자와 상의해 **온라인/오프라인 감지 인프라만 먼저** 만들고, 실제 폴링 실패 처리는 FE-3에서 이 인프라를 재사용하는 것으로 범위를 좁혔다. `lib/useOnlineStatus.ts`(신규) — `ThemeToggle.tsx`가 이미 쓰던 `useSyncExternalStore` 패턴을 재사용해 `navigator.onLine` + `online`/`offline` 이벤트를 SSR-안전하게 구독. `Microsite.tsx`의 Q&A·설문 버튼에 연결해 오프라인이면 비활성 스타일(`opacity 0.45`, `cursor: not-allowed`, `aria-disabled`) + "오프라인 상태 — 연결되면 다시 시도하세요" 배너를 노출하고, 온라인 복귀 시 이벤트 기반으로 자동 재개된다. 아젠다·자료 목록은 애초에 네트워크를 안 타는 로컬 state라 "계속 보임"은 별도 캐싱 없이 이미 참 — 완료 기준 세 개(아젠다·자료 유지 / 참여 기능 비활성+사유 / 복구 시 자동 재개) 전부 충족. Playwright의 `context.setOffline(true/false)`로 실제 브라우저 오프라인 이벤트를 발생시켜 헤드리스로 실측(온라인 opacity 1 → 오프라인 opacity 0.45·cursor not-allowed·배너 표시 → 복귀 시 원상복구), 아젠다는 오프라인 중에도 정상 렌더 확인. lint·build·test 전부 통과, 콘솔 에러 0건 | `lib/useOnlineStatus.ts`, `components/Microsite.tsx` (2026-08-24 [[log]]) |
| FE-3 | Q&A 화면 연결 | 완료. `PUBLIC_EVENT_ID=1` 하드코딩(스튜디오 목업 id와 D1 실제 id가 연결된 적이 없어서 생긴 임시값)을 없애고, 참가자 공개 페이지(`app/[slug]/page.tsx`, 신규)가 BE-7 공개 조회 응답의 실제 이벤트 `id`를 `Microsite`→`QaPanel`까지 필수 prop으로 전달하도록 바꿈 — 인증(BE-12·13·FE-15)을 기다릴 필요 없이 BE-7이 이미 무인증·배포 완료였던 것을 D1 시드·라우트 코드로 확인 후 착수. Codex 단독 리뷰([[Reviews/2026-09-01-codex-fe-qa]]) P2·P3도 함께 반영: 폴링 `cache:'no-store'`+겹침 방지, 패널 열릴 때 textarea 포커스 자동 이동, 오류 `role="alert"`, 전송 성공 시 응답 질문을 목록에 즉시 반영. **완료 기준 3개 전부 헤드리스 브라우저로 실측**: ① 다른 브라우저에서 남긴 질문이 폴링으로 나타남 — 두 탭을 열어 한쪽에서만 질문 등록 후, 이미 Q&A 패널이 열려 있던 다른 탭이 재마운트 없이 4초 인터벌 폴링만으로 새 질문을 잡아내는 것 확인(마운트 시 최초 로드가 아니라 반복 폴링 자체가 동작함을 별도로 검증). ② rate limit 초과 시 사유 노출 — 60초 3건 한도를 실제로 넘겨 429를 유발, `role="alert"`로 "잠시 후 다시 시도해 주세요. 질문은 60초에 3건까지 남길 수 있습니다" 노출과 전송 버튼 60초 비활성(dimmed) 확인. ③ 확대 없이 읽고 누를 수 있음 — 버튼 48px·텍스트영역 최소 44px·본문 13.5px 이상(터치 타깃 44px 이상 기준 충족, 기존 WIP 설계 그대로). 없는 slug는 `notFound()`로 404, 스튜디오 자체 프리뷰(에디터/뷰어)는 회귀 없음. lint·test(12건)·build 통과 | `app/[slug]/page.tsx`, `components/{Microsite,QaPanel}.tsx`, `lib/api.ts`, `components/screens/{Editor,Viewer}Screen.tsx` (2026-09-01 [[log]]) |
| BE-5 | 이벤트 로그 적재 + OPS 집계 | 완료. `POST …/logs`(배치 30건, `kind` 4종, `session_view`·`doc_view`는 대상 id 필수) + `GET …/ops`(방문자·열람·설문완료, 세션/자료별 `visitors`+`hits`, `attendanceRate` 상한 1). visitor는 토큰 파생이라 **같은 사람의 열람이 하나로 세어지고, 토큰 없으면 거절이 아니라 `visitor` NULL**(로그는 참여 기능이 아니라 계측이라 화면을 막지 않는다). rate limit은 `lib/rate-limit.ts` 재사용하되 한도를 Q&A보다 넉넉히(마이그레이션 0005로 키 컬럼·인덱스 추가). **30일 보존 삭제를 자정 Cron에 추가** — 지금은 시드 리셋이 CASCADE로 쓸어가지만 계정이 생기면(BE-12) 이 정리만이 유일한 상한이라 리셋에 기대지 않는다. 시드에 예시 로그 13건 추가(FE-5가 빈 화면이 아니게) | PR #17(머지), [[API-Guide-FE]] (2026-09-02 [[log]]) |
| BE-7 | 참가자 화면용 공개 조회 API (slug 기반) | 완료. `GET /api/public/[slug]` 단일 응답(이벤트+아젠다+자료+테마+engage+이벤트 id). 공개 3종 상태만 노출하고 초안·없는 slug 모두 동일한 404(존재 비노출), `r2_key`는 경계 밖. rate limit 대신 `Cache-Control: max-age=10`으로 흡수(D1 판정형 읽기 제한은 역효과). **잔여였던 `Microsite` 실제 렌더는 FE-3이 이 API를 소비하는 `/[slug]` 페이지를 만들며 해소** — 프로덕션에서 라우트·API 200 확인(시각 확인은 FE 담당자 실측 기록 참조). 이후 BE-16에서 공개 상태 판정을 공통 계층으로 승격하며 자기 상수를 버림 | PR #10(머지), [[API-Guide-FE]] (2026-08-26 [[log]]) |
| BE-11 | 2차 코드리뷰 지적 10건 반영 | 완료. 정확성 5건(`null` 본문 400·`createdAt` ISO+Z·시드 세션 id 고정·일일 캡 KST 경계·자정 경합 FK 위반 404) + 효율 2건(`created_at DESC` 인덱스·`SELECT` 컬럼 명시) + 구조 3건(`ApiError`/`withRoute`·워커 엔트리 스프레드·시드 slug 파생). **잔여였던 '자정 리셋 후 세션 id 1~6 유지'를 프로덕션에서 확인**(2026-09-02, 여러 차례 리셋 뒤에도 `[1,2,3,4,5,6]`) | PR #8(머지) (2026-08-26 [[log]]) |
| BE-14 | 아젠다·자료 쓰기 API | 완료. `PUT …/sessions`·`PUT …/documents` — 목록 전체를 받는 **id 기준 diff**(전체 삭제 후 재삽입이면 세션 id가 새로 발급돼 questions·survey_responses·documents의 `session_id`가 FK SET NULL로 조용히 끊긴다). 배열 위치가 곧 `sort_order`. `GET /api/events/[id]`에 `documents` 추가 + 3쿼리 batch. **세션 삭제 시 `idx_survey_once` UNIQUE 위반**(PR #12 리뷰가 재현)을 그 세션의 설문 응답을 함께 지우는 것으로 해결 — 근거는 UNIQUE 회피가 아니라 의미(`SET NULL`이면 세션 평가가 행사 전체 평가로 둔갑). **잔여**: 스키마 `ON DELETE CASCADE`로 이관(BE-4의 0003과 적용 순서로 얽혀 미뤘다), 소유권 검사(BE-13) | PR #14(머지), [[API-Guide-FE]] (2026-08-31 [[log]]) |
| BE-21 | rate limit 카운터 테이블 구현 | 완료. 마이그레이션 0008 `rate_counters(key_hash, scope, window_key, count, expires_at)` + `lib/rate-limit.ts` 판정을 대상 테이블 스캔에서 **PK 정확 매칭 ≤4행**으로 교체(EXPLAIN 실측: `SEARCH … USING INDEX (key_hash=? AND scope=? AND window_key=?)` vs 예전 `created_at>?` 범위 스캔). **증가는 성공한 쓰기와 같은 batch**에서만(ADR 0008). `scope`에 정책+범위를 담아 BE-19의 이벤트별/전역 구분을 그대로 유지. 부수 효과로 **설문 재제출이 이제 60초 창에도 잡힌다**(예전엔 행 기준이라 못 봤다) — `countWrites` 우회 대응이 필요 없어져 제거. 만료 정리를 자정 Cron에 추가 | PR #29(머지), [[0008-rate-limit-counter]] (2026-09-07 [[log]]) |
| BE-2 | Cloudflare 배포 — 살아있는 링크 확보 | 완료. https://sympo-studio.fomula91.workers.dev — 공개 URL에서 앱이 뜨고 D1·R2 바인딩 동작(`env.DB`·`env.DOCS`·`env.ASSETS`), Cron `0 15 * * *` 등록. **push→자동 배포는 GitHub Actions로**(`ci.yml` 검증 게이트 + `deploy.yml`) — Workers Builds 대신 고른 이유는 public 저장소 무료 무제한과 **lint·test·build를 배포 게이트로 강제**할 수 있어서다(카드가 붙은 계정에 과금 축을 더 늘리지 않는 것도 이유). 시크릿 등록 후 **첫 자동 배포 성공 실측**(버전 `08d0a8e9`). 원격 D1 마이그레이션은 의도적으로 자동화하지 않았다 — 배포는 되돌릴 수 있지만 마이그레이션은 아니다. 완료 기준의 "결제 수단 미등록"은 BE-6의 R2 활성화로 무효가 돼 **"사용량 알람 + 코드 상한"**으로 대체했고 둘 다 확인 | PR #31(머지), [[log]] 2026-08-24·09-07 |
| BE-17 | rate limit을 (키, 창) 카운터로 전환할지 결정 | **결정 완료 → [[0008-rate-limit-counter]]**. 무료 티어 한도와 대조하니 남은 3건의 크기가 전혀 달랐다: ① 무효 요청은 **Workers 요청 한도(10만/일)가 D1보다 먼저 걸려** D1을 소진할 수 없고 카운터로도 못 막는다(요청이 이미 도착한 뒤다) ② 60초 창 해머링은 하루 쓰기 캡이 이미 상한 ③ **판정 스캔만이 실제로 읽기 티어를 소진**하며 공격이 아니라 정상 운영(행사장 단일 IP)에서 밟힌다. **③ 때문에 도입**하되 **성공 쓰기에만 증가**(요청마다면 쓰기 티어가 먼저 터져 Q&A·설문·로그가 전부 죽는다). ADR 0006의 KV 기각은 **저장소 추가**를 겨냥한 것이라 D1 테이블 카운터에는 해당하지 않음을 확인. **BE-17의 완료 기준이 카운터로 풀리지 않는 것을 요구하고 있었다는 점도 정정.** 구현은 BE-21, 무효 요청은 BE-22(엣지)로 분리 | PR #28(머지), [[0006-rate-limit-key]] 부분 개정 (2026-09-07 [[log]]) |
| BE-20 | 공개 조회 API가 추출 프리셋의 색을 안 돌려줌 | 완료. **원인이 응답 형태보다 깊었다** — `brand_presets`가 0001부터 있었으나 **쓰기 경로가 없어 내내 비어 있었고**, 그래서 `events.preset_id`(FK) 설정이 전부 500이었다(실측). ① 빌트인 5종을 **마이그레이션 0006**으로 심음(시드가 아니라 참조 데이터라 자정 리셋에 지워지면 안 된다) ② `GET·POST /api/presets` 신설(업서트 — 추출은 값이 흔들려 회차마다 새 id를 만들면 목록이 같은 브랜드로 찬다) ③ 공개 응답 `theme.preset`에 `h`·`c`·`label`을 LEFT JOIN으로 실음(왕복 안 늘림, `presetId`는 유지해 계약 비파괴) ④ 없는 프리셋 PATCH를 500→**400 사유 노출** | PR #26(머지), [[API-Guide-FE]] (2026-09-07 [[log]]) |
| BE-19 | PR #9 최종 코드리뷰가 발견한 BE 잔여 지적 4건 | 완료. ① **rate limit 범위를 층마다 다르게** — 브라우저 토큰 버킷은 이벤트별로, IP 총량 상한은 전역으로. 전자는 "사람의 제출 속도"라 행사가 바뀌면 다시 세는 게 맞고(전역이면 한 행사에서 3건 쓴 사람이 다른 행사 첫 질문부터 429), 후자는 위조 방어라 전역이어야 의미가 있다(이벤트별이면 봇이 이벤트를 여러 개 만들어 곱하기 우회 — 운영자 CRUD가 아직 무인증). **토큰 없는 강등 버킷은 전역 유지** — 좁히면 토큰 생략이 곧 우회다. ② `responseRate`에 하한(`Math.max(0,…)`) + `capacity` 음수를 POST·PATCH 양쪽에서 400. ③ `GET /api/public/[slug]`를 slug 서브쿼리 batch로 묶어 **왕복 3→1**(참가자 화면의 가장 트래픽 많은 경로). ④ FK 위반→404 catch 5곳과 "이벤트 없음" 문구 복제를 `lib/db.ts`의 `isMissingEventFk`·`eventNotFound`·`assertEventFound`로 공용화 | PR #25(머지) (2026-09-07 [[log]]) |
| BE-16 | 참가자 쓰기 라우트의 공개 상태 검사 | 완료. `PUBLIC_STATUSES`·`assertPublicEvent`를 `lib/db.ts`로 승격하고 Q&A GET·POST와 설문 POST에 적용 — BE-7도 자기 상수를 버리고 공통 것을 쓴다. 비공개는 **없는 이벤트와 같은 404**(존재 비노출), 상태 게이트가 engage 토글보다 먼저다(403으로도 존재를 알려주지 않게). `survey/summary`는 운영자용이라 예외로 두고 근거를 주석에 남김 | PR #16(머지), PR #9 교차 리뷰 Codex 발견 (2026-09-02 [[log]]) |
| FE-5 | 리포트를 실측 데이터로 교체 | 완료(범위 좁힘 — 사용자와 상의 후 결정). 착수 전 확인: 스튜디오의 `ReportScreen`이 받는 `ev`는 `useStudio()`의 로컬 목업 이벤트(`seedEvents()`가 만드는 0~14번)라 D1의 실제 데모 이벤트(`id=1`)와 연결된 적이 없다(FE-19/BE-20과 같은 근본 원인). 스튜디오 미리보기가 실제 D1에 접근하던 것은 바로 이 세션 직전에 버그로 고친 패턴(Q&A P1)이라, 로컬 목업의 `ev.id`를 그대로 실제 이벤트 id로 써서 `/ops`를 부르면 그 버그를 리포트 화면에 새로 만드는 셈 — 그래서 스튜디오 `/report`(목업 미리보기)는 손대지 않고 그대로 두고, FE-3의 `/[slug]` 패턴처럼 **실제 공개 이벤트 전용 리포트 뷰를 별도 라우트로 추가**하는 것으로 사용자와 합의. `app/[slug]/report/page.tsx`(신규) — `GET /api/public/[slug]`로 이벤트 상세(제목·장소·예상인원·세션)를, `GET /api/events/[id]/ops`(`lib/api.ts`에 `fetchEventOps` 추가)로 실측 집계를 받아 방문자·페이지뷰·설문완료·참석률 스탯과 세션별 열람(방문자수/예상인원 기준 %, 인원 미설정 시 인원수 폴백) 막대를 그린다. 새 BE 작업 불필요 — `/ops`는 이미 배포돼 있고 운영자 조회는 상태 무관하게 열려 있다(BE-19가 인증 부재를 기존 갭으로 이미 등록해둠). 기존 `ReportScreen`의 METRICS/OPS(도구 자체의 빌드 효율 지표, 이벤트 방문 데이터가 아님)는 그대로 샘플로 유지 — 실측 대상이 애초에 아니었음을 확인. 로컬 D1에 세션 2건·로그 5건을 임시로 넣어 실데이터 렌더 확인 후 정리, 헤드리스 브라우저로 실제 방문자 2명·페이지뷰 2·설문완료 1건·참석률 2%·세션별 열람 막대가 정확히 그려지는 것과 존재하지 않는 slug의 404를 확인. lint·test(12건)·build 통과 | `app/[slug]/report/page.tsx`, `lib/api.ts` (2026-09-02 [[log]]) |
| BE-10 | 질문 숨김(모더레이션) 경로 | 완료. `PATCH /api/events/[id]/questions/[qid]` `{status: published\|hidden}` — 지우지 않고 상태만 바꿔 되돌릴 수 있게. `event_id`를 WHERE에 함께 둬 남의 행사 질문을 id만으로 못 건드린다. **`engage_qa` off 정책 결정**: 목록 조회는 계속 서빙한다(토글은 "새 질문을 받는가"이지 "이미 받은 질문을 감추는가"가 아니다) | PR #16(머지), [[API-Guide-FE]] (2026-09-02 [[log]]) |
| BE-8 | Q&A 폴링 GET 남용 통제(읽기 쪽) | 완료. GET에 batch(왕복 1회)·`Cache-Control: max-age=3`·`?since` 커서·ETag/304, POST는 이벤트 조회와 rate limit 판정을 batch로(왕복 2→1). `survey/summary`에 `max-age=5`. **읽기 티어를 실제로 줄이는 건 엣지 캐시뿐이고 ETag는 바이트만 아낀다**는 구분을 기록. 배포(`4e1d118d`) 후 프로덕션 실측. 닫지 못한 3건(무효 POST 미제한·60초 창 해머링·판정 2000행 스캔)은 같은 뿌리라 BE-17(main, rate limit 키·창 전환 ADR)로 이관 | PR #15(머지), [[API-Guide-FE]] (2026-09-02 [[log]]) |
| ~~BE-18~~ | Q&A·설문 라우트가 engage 토글만 보고 이벤트 상태(공개 여부)는 안 봄 | **폐기(중복).** Codex CLI 교차 리뷰가 이 브랜치에서 발견해 등록했으나, `origin/main`의 BE-16이 정확히 같은 문제를 이미 고쳐 배포했음을 병합 중 확인 — 별도 조치 불필요, 번호만 결번 처리 | main BE-16과 동일 건, [[Reviews/2026-09-02-pr9-final-review]] X1 (2026-09-02 [[log]]) |
| BE-4 | 설문 응답 수집 + 집계 | 완료. 배치 제출(`x-client-token` 필수)·재제출 upsert·`GROUP BY` 집계·응답률. rate limit을 `lib/rate-limit.ts`로 공통화(BE-3 동작 불변). **PR #12 리뷰 지적 4건 반영**: 판정 해시 비덮어쓰기(누적 `write_count` 귀속 이동으로 세탁·과대계상 양방향), `responseRate` 상한 1, `created_at`/`updated_at` 분리 + 판정 컬럼을 `RatePolicy.timeColumn`으로. 마이그레이션 0003·0004 원격 적용, 배포(`f3a9505f`) 후 **프로덕션 실측** | [[0006-rate-limit-key]], PR #12(머지), [[API-Guide-FE]] (2026-08-27~31 [[log]]) |
| FE-16 | StudioProvider의 editingId를 URL에서 파생시키도록 재구조화 | 완료. `StudioState.editingId`(state로 따로 들고 effect로 URL과 사후 동기화하던 필드)를 아예 없애고, `StudioProvider`가 `useParams()`로 URL의 이벤트 id를 직접 읽어 `ev`를 매 렌더 동기적으로 계산하도록 바꿈 — effect 라운드트립이 사라져 하드 리로드 시 헤더·게이트가 잘못된(이전) 이벤트로 잠깐 보이는 문제가 구조적으로 없어짐. `patchEvent`·`resetSessions`도 `prev.editingId` 대신 URL 파생값을 참조하도록 교체(둘 다 `useCallback` deps에 `urlEventId` 추가). `app/(studio)/events/[id]/edit/page.tsx`는 자체 `ev` 계산 + 동기화 effect가 필요 없어져 `if (ev.id !== Number(id)) notFound();` 한 줄로 단순화(유효하지 않은 id의 404는 그대로 유지 — provider의 `ev`가 `s.events[0]`로 폴백해도 그 id가 URL의 id와 다르면 여기서 걸림). `ConsoleScreen`·`StudioShell`의 `patch({editingId: ...})` 호출 3곳은 이제 URL이 알아서 반영하므로 제거. 헤드리스 브라우저로 `/events/1/edit` 하드 리로드 시 첫 프레임부터 정확한 이벤트(AURORA) 헤더 확인, 콘솔 카드 클릭 SPA 네비게이션, "새 이벤트" 생성 플로우 모두 회귀 없음 확인. **교차 리뷰가 회귀 발견·즉시 수정**: URL에 이벤트 id가 없는 `/report`·`/console`에서 `ev`가 항상 `s.events[0]`로 떨어져 "마지막 편집 이벤트" 기억을 잃던 것 — `selectedId`를 effect가 아니라 렌더 중 조건부 갱신으로 URL과 동기화해(effect였다면 원래 버그가 재발) `effectiveId = urlEventId ?? selectedId`로 통일, 이벤트1 편집→리포트 이동→에디터 복귀 시 정확히 `/events/1/edit`로 돌아가는 것 재확인 | `lib/types.ts`, `components/StudioProvider.tsx`, `components/StudioShell.tsx`, `components/screens/ConsoleScreen.tsx`, `app/(studio)/events/[id]/edit/page.tsx` (2026-09-07 [[log]]) |
| BE-3 | Q&A 저장·조회 API + 남용 통제 | 완료. 저장·조회 왕복, 글자 수 제한, 60초 3건/일 30건 rate limit(429 사유 노출), 시드 리셋 Cron — 로컬·브라우저·**프로덕션 URL 원격 실측**까지. 두 차례 코드리뷰의 즉시 수정 15건 반영(BE-11), 구조 지적은 BE-8~10으로 분리. Cron `0 15 * * *` 원격 등록 확인(배포 출력) | PR #8(머지), `lib/qa.ts`·`worker/index.ts` (2026-08-24~26 [[log]]) |
| BE-9 | rate limit 키를 NAT 환경에서 안전하게 | 완료. `x-client-token` 브라우저 버킷(60초 3건/일 30건) + IP 총량 상한(60초 20건/일 300건)의 2층 구조, 토큰 없으면 IP 단독 버킷 강등. 완료 기준 2건을 로컬·**프로덕션 양쪽 실측**(같은 IP 토큰 2개 독립 한도, 위조 토큰 IP 상한 429). 마이그레이션 0002 원격 적용 + 배포 완료. FE-3에 토큰 전송 요구 반영 | [[0006-rate-limit-key]], PR #11(머지), [[API-Guide-FE]] (2026-08-26 [[log]]) |
| FE-21 | 오프라인 중 발생한 page_view·session_view가 재연결 후에도 유실됨 | 완료(등록 직후 같은 세션에서 바로 처리 — 생각보다 작은 변경이라 판단해 FE-20과 함께 마무리). `sendEventLogs()`를 `Promise<boolean>`로 바꿔 성공 여부를 알려주게 하고, `page.tsx`는 실패 시 `pageViewSent.current`를 되돌리고 `Microsite.tsx`는 실패한 id를 `seen`에서 뺀다 — 새 재시도 인프라 없이 **FE-9가 이미 만든 "재연결 시 재조회 → effect 재실행" 경로에 얹혀 자동 재시도**된다. 실제 네트워크 요청으로 `sendEventLogs`가 유효한 이벤트엔 `true`, 존재하지 않는 이벤트(404)엔 `false`를 정확히 반환하는 것을 확인(IntersectionObserver 자체는 이 세션의 Chrome 창이 `document.hidden` 고정이라 재검증 불가, FE-20과 같은 환경 한계). lint·test(12건)·build 통과 | PR #22, `lib/api.ts`·`app/[slug]/page.tsx`·`components/Microsite.tsx` (2026-09-06 [[log]]) |
| ~~FE-22~~ | React 컴포넌트/컨텍스트 자동 테스트 인프라 도입 | **폐기(리젝).** `@testing-library/react`+`jsdom`+`vitest.config.mts`로 구현해 PR #33 제출, Codex CLI·Claude 교차 리뷰 둘 다 지적 없음이었으나 fomula91이 "테스트 인프라 지금 필요없음"으로 리젝 — 품질 문제가 아니라 우선순위 판단. 번호만 결번 처리, 재등록하지 않음 | PR #33(닫힘, 미머지) (2026-09-08 [[log]]) |
| FE-6 | PDF.js 뷰어 + 현장 업로드 + 가상 강의자료 제작 | 완료(범위 축소 — 사용자와 상의 후 결정). 업로드 UI는 스튜디오가 D1 이벤트에 연결된 적이 없어(FE-5·FE-19·FE-23과 같은 구조적 원인) 착수 전 뷰어만 먼저 하기로 정하고, 업로드는 **FE-25로 분리**. **뷰어**: `pdfjs-dist`로 `components/PdfViewer.tsx` 신설(canvas 렌더, 페이지 이동·확대 버튼, `next/dynamic(ssr:false)`로 초기 번들 제외 — iframe은 iOS Safari에서 깨져 기각, FE-6 완료 기준대로). 백엔드(`GET /api/public/[slug]`)는 이미 문서마다 서명 URL을 내려주고 있었는데 `app/[slug]/page.tsx`가 그 필드를 매핑에서 버리고 있었다 — `DocumentInfo.url` 추가로 한 줄만 이어 붙이면 됐다. `Microsite.tsx`의 자료 카드에 클릭 핸들러 연결(`role="button"`·키보드 접근 추가), 열 때 `doc_view` 로그 전송(FE-20이 "여는 동작이 없어 보류"로 남겨둔 잔여 항목을 트리거가 생긴 김에 마저 연결). 스튜디오 미리보기(`DEMO_DOCUMENTS`)도 `pdf-lib`로 생성한 가상 강의자료(`public/demo/sample-lecture.pdf`, 4쪽)를 가리켜 실제로 열어볼 수 있다. **실측**: curl/node fetch로 실제 이벤트(`id=1`)에 문서 생성→업로드까지 API로 직접 호출해(운영자 UI 없이도 계약은 성립) 서명 URL·`content-type: application/pdf` 응답 확인, 브라우저로 열어 페이지 1/4 렌더·다음 페이지 이동(`2 / 4`로 갱신) 확인, D1에 `doc_view` 로그 2건 적재 확인 후 테스트 데이터 정리. **모바일 뷰포트의 실제 화면 캡처는 이 세션 브라우저 자동화 도구가 반복적으로 불안정해(스크린샷 타임아웃·해상도 오보고) 완결하지 못했다** — 반응형 레이아웃(flex·44px 터치 타깃·`overflow:auto`)은 적용했으나 실기기/에뮬레이터 확인은 남은 채로 종료. PDF.js 타입 이슈 하나 실측으로 정정: `getDocument().promise`가 반환하는 문서 프록시엔 `destroy()`가 없고 `doc.loadingTask.destroy()`가 맞는 API였다(런타임 검사로 확인, 문서 접근이 `node_modules` 차단으로 막혀 있어 직접 실행해 확인). `npm run lint`·`test`(23건)·`build` 통과 | `components/PdfViewer.tsx`, `components/Microsite.tsx`, `lib/types.ts`, `app/[slug]/page.tsx`, `public/demo/sample-lecture.pdf`, FE-25 (2026-09-14 [[log]]) |
| FE-4 | 설문 흐름 재설계 + BE-4 연결 | 완료. **구조 결정**: `field-experience.md`가 "세션별 1문항 즉시"를 "설계 직관일 뿐 실측 없음"으로 이미 못박아둔 상태라, 사용자와 상의해 채택하지 않기로 결정 — 세션 종료 감지 트리거를 새로 설계해야 하고 인터럽션 빈도가 오히려 고령 사용자를 더 피로하게 할 위험이 있는데 그걸 감수할 근거가 없었다. 대신 **2단 설문 구조를 유지하고 UI만 고령 친화적으로 재설계**(큰 버튼·큰 글씨·문항 최소화)하는 쪽으로 결정, `field-experience.md`·README 양쪽에 근거를 남김. **구현**: `SurveyPanel.tsx`(신규) — 1단계 `overall_satisfaction`(행사 전체 만족도, 1~5), 2단계 세션별 `session_rating`(전부 선택, "평가하고 싶은 세션만 답해도 됩니다" 안내) — 두 단계를 한 배치로 모아 제출 시점에 `POST /api/events/[id]/survey` 한 번만 호출. 완료 화면에서 이름(선택) 입력 후 `generateCertificate()`(기존 구현) 호출로 수료증 다운로드까지 이어짐 — **완료 기준이 요구하는 "참가자 흐름에서 실제 호출"을 충족**(기존엔 에디터 미리보기 버튼에서만 호출됐다). `lib/api.ts`에 `submitSurvey()` 추가(기존 `fetchWithTimeout`·`getClientToken` 재사용), 성공 시 `sendEventLogs(eventId, [{kind:'survey_complete'}])`로 FE-20이 보류해뒀던 `survey_complete` 로그도 함께 연결. `Microsite.tsx`의 정적 placeholder("설문 참여 · 2분" div)를 `QaPanel`과 같은 열기/닫기 버튼 패턴으로 교체, `preview` 모드에서는 비활성 안내만 표시(실제 D1에 쓰지 않음). 헤드리스 브라우저로 실제 데모 이벤트에서 1단계→2단계→제출(201)→`survey_complete` 로그 전송(201)→수료증 다운로드까지 전체 흐름 실측(테스트 중 로컬 D1이 최근 머지된 BE-21 마이그레이션(0008 rate_counters)을 못 받아 500이 났던 것은 `npm run db:migrate`로 해결 — 코드 문제 아님). 테스트 데이터는 정리 완료. README의 "설문이 화면에 연결되지 않았다"·"구조 재검토가 남았다" 서술을 실제 상태로 갱신하되, **응답률 가설 자체(고령 사용자가 실제로 더 응답하는가)는 여전히 실사용 데이터 없이는 증명되지 않음을 명시적으로 유지**(완료 기준 요구사항). 이메일 발송은 이번에도 구현 안 함(다운로드로 대체, 계획대로). `npm run lint`·`test`(12건)·`build` 전부 통과 | `components/SurveyPanel.tsx`, `components/Microsite.tsx`, `lib/api.ts`, `README.md`, `llm-wiki/Reference/field-experience.md` (2026-09-09 [[log]]) |
