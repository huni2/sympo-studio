# sympo-studio — Log

이 프로젝트의 시간순 기억. 최신이 위로.

> **형식 계약 (훅이 파싱한다)**: 날짜 섹션은 `## YYYY-MM-DD`, 항목은 `- **[태그] 제목**: 내용`.
> 코드 repo의 SessionStart 훅이 최신 섹션의 **제목**들만 추출해 세션에 주입한다(태그 포함).
> 제목은 주입만 보고도 무슨 일이 있었는지 알 수 있게 쓴다.
>
> **담당 태그** — 여러 사람이 작업하므로 항목마다 하나씩 붙인다. [[Onboarding-FE]] 참조.
> `[FE]` 프론트엔드 코드 · `[BE]` 백엔드·인프라 코드 · `[PROJ]` 위키·문서·설정 등 코드 외 작업.
> 한 항목이 FE와 BE를 모두 건드렸다면 **항목을 쪼갠다** — 태그를 두 개 붙이지 않는다.

> **과거 기록은 아카이브에 있다** — 이 파일은 최근 3개 날짜만 둔다(훅은 최신 섹션만 읽는다).
> [[Archive/log-2026-09]] · [[Archive/log-2026-08]] · 사람이 읽을 연표는 [[Summaries/History]]

## 2026-09-21
- **[FE] FE-15 — 게스트 로컬 워크스페이스 영속 + 로그인/계정 위젯 + 콘솔을 실제 D1에 연결(묶음 1)**: 재편이 지목한 다음 착수 대상. 사용자가 실측으로 짚은 "새 이벤트를 만들면 15→16건, 새로고침하면 다시 15건" 증상을 정면으로 고쳤다. **게스트 워크스페이스**는 `StudioProvider.tsx` 마운트 이펙트에서 `GET /api/auth/me`로 로그인 여부를 먼저 확인하고, 비로그인이면 `localStorage`(`sympo-guest-events-v1`)를 읽어 교체한다 — **SSR과 첫 클라이언트 렌더는 항상 기존 시드로 시작**해 하이드레이션 불일치를 피하고, 실제 교체는 마운트 이펙트(클라이언트 전용) 안에서만 한다. 로컬 생성 이벤트에 `localRef`(`crypto.randomUUID()`)를 심어 FE-39가 나중에 멱등 가져오기를 얹을 토대를 남겼다. **로그인 위젯**: 사이드바 하단의 정적 "OP" 자리표시자를 걷어내고 `components/AccountMenu.tsx`를 새로 만들었다 — 게스트는 `/api/auth/google?next=<현재 경로>` 전체 이동 링크, 로그인 사용자는 아바타+드롭다운(이름·이메일·로그아웃). **콘솔↔D1 연결**: `lib/studio-api.ts`에 `fetchCurrentUser`·`fetchStudioEvents`·`createStudioEvent`·`logoutStudioUser`를 추가(전부 이미 있는 BE-12·13·25 엔드포인트를 부르는 순수 배선), 컨텍스트에 `createEvent()` 액션을 신설해 `StudioShell.tsx`의 "새 이벤트" 버튼이 로그인 여부로 갈리게 했다. **서버가 `brand`를 필수로 요구하는데 이 범위엔 브랜드명 입력 필드가 없어** 제목으로 채웠다(알려진 임시 결정, 별도 과제로 등록할 만큼 크지 않다고 판단해 기록만 남김). **실측(게스트, 브라우저)**: 새 이벤트 생성 → 16건 → **`location.reload()` 하드 리프레시 후에도 16건 유지** → 콘솔에서 다시 열어 편집 화면 정상 진입 → 다크모드에서도 배너·위젯 정상. 콘솔 경고 1건("id는 양의 정수여야 합니다")은 `Date.now()` id에 대한 FE-30의 기존 조회 시도가 400을 받는 것인데 `isKnownLocally` 가드가 이미 흡수해 화면엔 무해(신규 결함 아님, 코드로 확인). **미실측**: 로그인 상태의 생성·목록 왕복, 다른 계정 격리, 로그아웃 복귀 — 이 환경에 실제 Google 계정 왕복 수단이 없어 타입 검사·빌드·서버 계약(BE-12·13·25 라우트 재확인)으로 대신했다. `npm run lint`·`build` 통과.
- **[BE] `/api/auth/google`이 설정 누락 에러를 raw JSON으로 사용자 화면에 그대로 노출하던 것을 콜백 라우트와 같은 리다이렉트 패턴으로 정정**: 사용자가 로컬에서 로그인 버튼을 눌러 직접 확인했다 — `.dev.vars`에 `GOOGLE_CLIENT_ID`/`SECRET`이 없어 `googleConfig()`가 `ApiError`(503)를 던지는데, `withRoute`가 이걸 무조건 `json({error, status})`로 직렬화해 **브라우저가 그 JSON을 화면에 그대로 찍었다**(`{"error":"Google 로그인이 아직 설정되지 않았습니다..."}`). 이 라우트는 `fetch`가 아니라 `<a href>`로 직접 여는 용도라 API 에러 포맷이 맞지 않는다. 같은 로그인 흐름의 콜백 라우트(`/api/auth/callback/google`)는 이미 이 문제를 알고 실패 시 `/console?auth=cancelled` 같은 리다이렉트로 처리하고 있었다 — 그 패턴을 그대로 맞췄다: `googleConfig()` 호출을 `try/catch`로 감싸 실패하면 `/console?auth=config_error`로 302 리다이렉트한다. **문구는 URL에 싣지 않고 사유 코드만 넘긴다** — 화면 쪽이 문구를 정한다. `npm run lint`·`build` 통과.
- **[FE] 콘솔 화면이 로그인 실패 사유(`?auth=`)를 읽어 배너로 보여주게 함 — 콜백 실패도 지금까지 조용히 묻히고 있었다**: 위 BE 수정과 짝. 콜백 라우트의 기존 `cancelled`·`failed` 리다이렉트도 **그동안 프론트에서 읽는 코드가 하나도 없어서** 로그인이 실패하면 아무 설명 없이 콘솔로 돌아가기만 했다(아무도 이 경로를 눌러본 적이 없어 안 드러났던 결함). `ConsoleScreen.tsx`에 `useSearchParams()`로 `auth` 쿼리를 읽어 `cancelled`/`failed`/`config_error` 셋을 문구로 매핑해 danger 톤 배너로 보여주고, 보여준 뒤에는 `router.replace('/console')`로 쿼리를 지워 새로고침해도 안 남게 했다. **정적 프리렌더 페이지에서 `useSearchParams()`를 쓰면 Suspense 경계가 없어 빌드가 실패한다** — `ConsolePage`에서 `<ConsoleScreen>`을 `<Suspense fallback={null}>`로 감싸 해결(실측: 이 경계 없이 빌드해 실패 재현 후 추가해 통과 확인). **실측**: `http://localhost:3000/api/auth/google?next=/console`을 직접 열어 raw JSON 대신 콘솔 화면에 빨간 배너로 문구가 뜨는 것 확인, URL이 `/console`로 정리되는 것 확인, 콘솔 에러 0건. `npm run lint`·`build` 통과.
- **[PROJ] 흐름 묶음 재편(PR #57) 머지 — 번호 3건 재매핑 + 누락 과제 2건 복원**: 재편 자체는 타당해 그대로 받았다 — "컴포넌트 하나 고쳤다"는 완료가 쌓여도 **운영자→참가자 흐름이 어디서도 안 이어진다**는 지적이고, 완료 기준을 "API가 성공했다"에서 **"화면에서 끝까지 쓸 수 있다"**로 바꾼 것이 핵심이다. 핵심 주장 둘을 코드로 검증했다: ① `PUT /api/events/[id]/sessions`는 **실제로 완전히 구현돼 있다**(`route.ts:33`, 전체 삭제·재삽입이 아니라 id 기준 diff라 세션 id가 보존된다) — 그런데 **FE에서 부르는 곳이 한 곳도 없다.** "순수 FE 배선만 남았다"는 FE-24 배정 근거가 맞다. ② FE-40(신규)의 주장도 맞다 — `EditorScreen.tsx:533`이 추출 프리셋을 로컬 `customPresets`에만 넣고 `POST /api/presets` 호출처가 없어, 그 프리셋을 이벤트에 걸면 `route.ts:164`가 400("먼저 저장하세요")을 낸다. **머지 전에 고친 것 넷**: ⓐ **번호 충돌 3건** — 이 PR은 오늘 오후 머지(#54·#55·#56) 이전 상태를 전제로 쓰여 FE-36/37/38을 전부 다른 의미로 쓰고 있었다. 브랜드 라벨은 main 번호 FE-37로 맞추고, 신규 2건(계정 분리·프리셋 저장)은 빈 번호 **FE-39·FE-40**으로 옮겼다. ⓑ **누락 2건 복원** — 재편 목록에 main의 FE-36(로컬 전용 이벤트 저장 문구)과 FE-38(참여 토글 문구 역순)이 **아예 없어서 그대로 머지하면 조용히 사라질 뻔했다.** 둘 다 "에디터가 말하는 저장 상태가 사실인가"라 묶음 1(생성 → 새로고침해도 유지 → 다시 편집)의 완료 기준과 같은 문제여서 거기 넣고, 완료 기준에도 "화면이 말하는 저장 상태가 실제와 일치"를 명시했다. ⓒ **묶음 0 상태가 낡음** — "FE-34·35만 머지되면 끝"이었으나 오늘 전부 머지돼 **완료**로 고쳤다(`Summaries/Next.md`도 같이). ⓓ **`[묶음 N]` 태그 누락 3건** — 복원·유지한 과제 제목에만 태그가 없어 형식이 어긋났다. 훅 파싱 14건·번호 중복 0건·태그 누락 0건으로 검증. 코드 변경 없음.
- **[PROJ] 위키 정리 — 사람용 층 2건·에이전트용 정본 1건·FE 진입 문서가 최대 7일 뒤처져 있던 것을 맞췄다**: PR 8건을 연달아 머지하면서 `log.md`·`Next-Tasks.md`는 매번 충돌 해소로 갱신됐지만 **나머지 층은 그대로 남아 있었다.** 진단부터 했다 — `log.md`는 3개 날짜 규칙 준수(09-21에 9건), `Next-Tasks.md`는 열린 과제 중복·종료 기록과의 겹침 0건으로 정상이었고, 낡은 것은 넷이었다. ① **`Summaries/Status.md`**(기준일 09-15, 6일 뒤처짐) — 기준일을 올리고 9/20~21에 닫힌 참가자 화면 결함 4건(FE-31·32·27·29)과 디자인 시스템(FE-13, 도구 화면 다크모드 갭)을 반영, 「무엇이 안 되는가」 맨 위에 FE-37을 올렸다. 아젠다·자료가 목업이라는 서술은 **여전히 사실이라 그대로 두고**, 화면이 이제 그 사실을 정직하게 말한다는 것만 덧붙였다(FE-34). ② **`Summaries/History.md`**(연표가 ⑩ 9/17에서 끊김) — ⑪ 「참가자 화면의 거짓말을 걷어내다(9/20~21)」를 추가했다. FE-28 기각 근거와, 번호를 두 번 연속 잘못 잡은 실패도 함께 적었다. ③ **`Context.md`**(기준일 09-14, main 해시 낡음) — 기준일·해시를 올리면서 **코드와 대조해 표류 3건을 잡았다**: 디렉터리 표에 `components/StudioApp.tsx`가 적혀 있었으나 **그 파일은 존재하지 않는다**(`StudioProvider.tsx`+`StudioShell.tsx`로 갈렸다), `screens/*`가 "전부 목업"이라 돼 있었으나 FE-30 이후 에디터 기본 정보는 D1에 붙었다(다만 화면 자체의 `fetch` 호출이 0건인 것은 **여전히 사실이라 그 표현으로 바꿔 살렸다** — 실제로 grep으로 재확인), API 라우트가 18개로 적혀 있었으나 실측 20개. `components/ui/*`(FE-13) 행도 새로 넣었다. ④ **`Onboarding-FE.md`** — "Next-Task는 FE-28 1건"과 "첫 과제로 FE-26·28·27·29"가 전부 종료·기각된 과제를 가리키고 있었다. FE-37(유일한 Next-Task)과 FE-38·FE-36(작고 서버 없이 되는 것)으로 바꿨다. **위키링크 무결성 검사**도 돌렸다 — 깨진 2종(`[[CLAUDE.md]]`·`[[feedback-minimize-subagents]]`)은 아카이브 원문의 것이고 이전 재편 때도 보존하기로 한 것이라 그대로 뒀다. 코드 변경 없음.
- **[PROJ] FE-28을 기각(판단 종료)으로 정정 기록 — PR #48이 머지 아닌 close라 그 안의 위키 수정이 반영되지 않고 있었다**: PR #48(FE-28)의 코멘트 스레드를 확인했다 — fomula91이 "이 PR의 전제(hits가 부풀어 순유효 방문자 수를 왜곡한다)가 코드로 보면 성립하지 않는다"고 지적했고, huni2가 코드로 재확인해 동의, **머지하지 않고 닫기로** 결론이 났다. 근거: ① 운영자 리포트(`app/[slug]/report/page.tsx`)는 `ops.visitors`·세션별 `row.visitors`만 읽고 `hits`·`documents[]`는 아예 안 쓴다 — 중복 로그가 쌓여도 운영자가 보는 숫자는 안 바뀐다 ② `visitors`는 `COUNT(DISTINCT visitor)`라 SQL에서 이미 중복 제거된다 ③ `hits`는 "몇 번"을 재도록 의도적으로 분리해둔 별개 지표다(`lib/ops.ts` 주석) — 억제하면 두 컬럼을 나눈 의미가 사라진다 ④ `session_view`의 억제 근거(`IntersectionObserver` 폭증)와 `doc_view`(명시적 클릭)는 성격이 달라 패턴을 맞출 이유가 없다. **문제는 그 판단이 PR 본문에만 있고 실제 위키엔 반영이 안 된 것**이다 — PR이 close라서 거기 포함됐던 `llm-wiki` 수정 커밋도 함께 버려졌고, `Next-Tasks.md`는 여전히 FE-28을 `🔴 필수` Next-Task로 들고 있었다(SessionStart 훅이 계속 주입 중이었다는 뜻). `Next-Tasks.md`에서 빼고 `Archive/Closed-Tasks.md`에 기각 사유를 코멘트 링크와 함께 남겼다. `Summaries/Next.md`도 "Next-Task 2건(FE-36·FE-28)" → "1건(FE-36)"으로 정정. 코드 변경 없음.
- **[PROJ] FE-38 등록(참여 토글 상태 문구 역순) + 내 과제 번호를 FE-36 → FE-37로 재정정**: PR #54(FE-34) 리뷰 중 인접 결함을 하나 찾았다 — `EditorScreen.tsx:423`의 `EngageSection`이 토글 즉시 `saved: '방금 저장됨'`을 찍는데 실제 `PATCH`는 700ms 디바운스 뒤라, 화면 문구가 **"방금 저장됨" → "변경 저장 중…" → "방금 저장됨"** 으로 뒤집힌다. 바로 옆 `BasicSection`(`:275`)은 순서가 옳아 **같은 파일 안에서 두 섹션이 다르게 하고 있다.** FE-34와 뿌리는 같지만 `engage`는 `detailPatchToBody`에 포함돼(`lib/studio-api.ts:103`) 실제로 저장되고 실패 시 정정되므로 데이터 손실이 없다 — 그래서 PR #54 범위("기존 필드 저장 흐름은 건드리지 않는다")를 지키고 FE-38로 따로 등록했다. 고치는 건 한 줄이다. **번호 재정정**: 어제 FE-35 → FE-36으로 옮긴 브랜드 라벨 과제를 다시 FE-37로 옮겼다 — PR #54가 이미 FE-36을 쓰고 있었는데 그때 #55만 확인하고 #54를 놓쳤다. 경위와 재발 방지는 FE-37 등록 항목에 적었다.
- **[PROJ] FE-37 등록 — 참가자 화면의 브랜드 이름이 실은 색 프리셋 라벨이다**: PR #52(FE-31) 리뷰 중 사용자가 "preset 문제를 코드베이스 기반으로 더 설명하라"고 해서 추적하다 찾았다. `app/[slug]/page.tsx:212`가 `brandLabel: preset.label`로 **색 프리셋 이름을 브랜드 이름 자리에 넣고** 있었고(`Microsite.tsx:233`이 상단에 그린다), `events.brand`는 생성 시 **필수 입력인데**(`app/api/events/route.ts:143`) 참가자 화면에 한 번도 안 나오고 운영자 콘솔 검색에서만 쓰였다(`ConsoleScreen.tsx:16`). `derive()`는 preset에서 `h`·`c`만 쓰므로(`lib/theme.ts:36`) `label`이 화면으로 새는 경로는 이 한 곳뿐이다. **FE-31이 이 결함을 가시화한다** — 여태는 추출 프리셋이 항상 slate로 폴백돼 "슬레이트 뉴트럴"이라는 무해한 고정값이 나왔는데, FE-31이 폴백을 걷으면 실제 label이 그대로 노출되고 **추출 프리셋 label의 초기값은 업로드 이미지의 파일명이다**(`EditorScreen.tsx:549`). 브라우저로 실측해 확정했다: label을 `logo-final-v3`로 둔 추출 프리셋을 걸자 참가자 화면 상단에 **`LOGO-FINAL-V3`**가 브랜드로 찍혔다(같은 이벤트의 `events.brand`는 `MERIDIAN`). 데이터는 이미 프론트에 와 있어(`GET /api/public/[slug]` 응답 최상위 `brand`, `PublicEvent.brand`로 선언까지 돼 있음) **BE는 할 일이 없는 순수 FE 과제**로 등록했다. 사용자에게 드러나는 결함이고 FE-31 머지로 활성화되므로 Next-Task(필수)에 올렸다. 사람용 층 `Summaries/Next.md`도 같이 갱신(Next-Task 1건 → 2건). **함께 짚었지만 등록하지 않은 것**: `/api/public/[slug]`가 `preset`과 `presetId`를 둘 다 내려주고 FE가 골라 쓰게 하는 계약은, `brand_presets`의 label·hue·chroma가 전부 NOT NULL이고 FK가 실제로 강제되므로(없는 id로 UPDATE하면 `FOREIGN KEY constraint failed`) `preset === null` ⟺ `presetId === null`이라 FE 폴백 체인의 가운데 항이 도달 불가능한 죽은 코드다 — BE가 `COALESCE(e.preset_id, 'slate')`로 조인하면 FE는 한 줄로 끝난다(BE-27의 "검사는 잊을 수 있지만 파생은 잊을 수 없다"와 같은 모양). 사용자가 과제 등록 범위를 FE-37로 한정해 기록만 남긴다. **번호 주의 — 같은 실수를 두 번 했다**: 처음 FE-35로 등록했다가 PR #55와 부딪혀 FE-36으로 옮겼고, 그마저 PR #54와 부딪혀 결국 FE-37이 됐다. 원인은 매번 같다 — **열린 PR 중 하나만 확인하고 번호를 골랐다.** 두 PR 다 `Next-Tasks.md`의 열린 목록에는 안 나타난다(#55는 발견 즉시 고쳐 종료 기록만 남겼고, #54는 아직 미머지였다). **번호를 잡기 전에 `llm-wiki/` 전체 + 열린 PR 전부를 스캔한다**(`gh pr diff <n> | grep -oE "FE-[0-9]+"`) — 열린 과제 목록만으로는 부족하다.
- **[PROJ] 열린 과제를 "컴포넌트 단위 완료"에서 "흐름 묶음"으로 재편 — 사용자가 브라우저 실측 4건을 근거로 지적**: 사용자가 직접 브라우저로 확인한 것을 근거로 문제를 제기했다 — 새 이벤트를 만들면 목록이 15→16건이 됐다가 새로고침하면 15건으로 돌아감(로컬 상태일 뿐 저장 안 됨) / 새 이벤트는 자료 0건인데 에디터엔 "자료 4"(`DocsSection`이 완전 정적 목업)·미리보기엔 샘플 PDF 2건이 뜸 / "공개하기"를 눌러도 공개 URL 발급이 아니라 미리보기 이동뿐(FE-23) / 운영자 리포트가 방금 만든 행사가 아니라 샘플 수치를 보여줌. **"컴포넌트 하나를 고쳤다"는 완료가 쌓여도 "운영자가 만든 행사를 참가자가 확인하는 흐름"이 어디서도 이어지지 않는 상태**였다. `Next-Tasks.md`의 "전체 조망"을 묶음 0~6(이미 작업한 브랜치 통합 → 행사 생성·재편집 → 아젠다 편집+공개 → 업로드+열람 → 브랜드 전달 → 참가자 동선 → 계정·구조 정리)으로 재편했다 — 번호는 다시 매기지 않고, 화면에서 끝까지 검증되는 단위로만 묶었다. **완료 기준을 "API 성공"과 "화면에서 끝까지 쓸 수 있다"로 구분**하는 원칙도 명시했다. **세 과제를 다시 스코프했다**: ① **FE-15가 너무 커서 쪼갰다** — 로그인·게스트 구분 + 콘솔 실제 D1 연결까지만 남기고, 가져오기·계정 관리는 **FE-37**(신규)로 분리. 로컬 고유 id(`localRef`)는 FE-15 단계에서부터 심어둬야 FE-37이 나중에 멱등하게 얹을 수 있다고 명시. ② **FE-23·FE-24를 발행 흐름으로 묶고, 아젠다 저장을 FE-24에 명시로 배정했다** — 요약 문서는 "FE-24 몫"이라 적어놓고 정작 FE-24 본문엔 세션 저장이 한 줄도 없던 것을 발견(과거 「다른 과제로 넘긴다고 로그에만 적고 끝났다」던 사고와 같은 모양). **조사 중 `PUT /api/events/[id]/sessions`가 diff 기반 upsert·삭제까지 이미 완전히 구현돼 있는 것을 확인**했다(BE-14) — 남은 건 순수 FE 배선뿐이다. FE-34는 이 배정과 무관하게 저장 문구만 정직하게 고친 것이라 실제 저장은 여전히 안 됨을 명시. ③ **FE-25에 리포트 진입점을 추가했다** — 운영자 리포트 전체 재설계를 기다리지 않고 기존 실측 화면 `/{slug}/report` 링크만 두면 업로드→열람→조회수 확인까지 화면으로 이어진다. **FE-14(FSD 재편)는 명시적으로 뒤로 미뤘다** — 데이터 소유권·저장 흐름이 묶음 1~4에서 아직 바뀌는 중이라 지금 옮기면 다시 옮기게 된다. **부수로 새 결함 하나를 발견해 등록했다**: `EditorScreen.tsx`의 `saveDraft()`가 추출한 브랜드 프리셋을 로컬 state에만 넣고 `POST /api/presets`(BE-25, 이미 구현됨)를 호출한 적이 없다 — FE-31 검증 때 로컬 D1에 직접 INSERT해야 했던 이유가 이것이었다. **FE-38**로 등록(묶음 4). `Summaries/Next.md`도 새 흐름 순서로 정리. 코드 변경 없음.
- **[PROJ] FE-36 등록 — 참가자 화면의 브랜드 이름이 실은 색 프리셋 라벨이다**: PR #52(FE-31) 리뷰 중 사용자가 "preset 문제를 코드베이스 기반으로 더 설명하라"고 해서 추적하다 찾았다. `app/[slug]/page.tsx:212`가 `brandLabel: preset.label`로 **색 프리셋 이름을 브랜드 이름 자리에 넣고** 있었고(`Microsite.tsx:233`이 상단에 그린다), `events.brand`는 생성 시 **필수 입력인데**(`app/api/events/route.ts:143`) 참가자 화면에 한 번도 안 나오고 운영자 콘솔 검색에서만 쓰였다(`ConsoleScreen.tsx:16`). `derive()`는 preset에서 `h`·`c`만 쓰므로(`lib/theme.ts:36`) `label`이 화면으로 새는 경로는 이 한 곳뿐이다. **FE-31이 이 결함을 가시화한다** — 여태는 추출 프리셋이 항상 slate로 폴백돼 "슬레이트 뉴트럴"이라는 무해한 고정값이 나왔는데, FE-31이 폴백을 걷으면 실제 label이 그대로 노출되고 **추출 프리셋 label의 초기값은 업로드 이미지의 파일명이다**(`EditorScreen.tsx:549`). 브라우저로 실측해 확정했다: label을 `logo-final-v3`로 둔 추출 프리셋을 걸자 참가자 화면 상단에 **`LOGO-FINAL-V3`**가 브랜드로 찍혔다(같은 이벤트의 `events.brand`는 `MERIDIAN`). 데이터는 이미 프론트에 와 있어(`GET /api/public/[slug]` 응답 최상위 `brand`, `PublicEvent.brand`로 선언까지 돼 있음) **BE는 할 일이 없는 순수 FE 과제**로 등록했다. 사용자에게 드러나는 결함이고 FE-31 머지로 활성화되므로 Next-Task(필수)에 올렸다. 사람용 층 `Summaries/Next.md`도 같이 갱신(Next-Task 1건 → 2건). **함께 짚었지만 등록하지 않은 것**: `/api/public/[slug]`가 `preset`과 `presetId`를 둘 다 내려주고 FE가 골라 쓰게 하는 계약은, `brand_presets`의 label·hue·chroma가 전부 NOT NULL이고 FK가 실제로 강제되므로(없는 id로 UPDATE하면 `FOREIGN KEY constraint failed`) `preset === null` ⟺ `presetId === null`이라 FE 폴백 체인의 가운데 항이 도달 불가능한 죽은 코드다 — BE가 `COALESCE(e.preset_id, 'slate')`로 조인하면 FE는 한 줄로 끝난다(BE-27의 "검사는 잊을 수 있지만 파생은 잊을 수 없다"와 같은 모양). 사용자가 과제 등록 범위를 FE-36으로 한정해 기록만 남긴다. **번호 주의**: 처음 FE-35로 등록했다가 FE-36으로 바꿨다 — 같은 날 먼저 올라온 PR #55가 `Next-Tasks.md`를 거치지 않고(발견 즉시 고치고 종료 기록만 남기는 경로) FE-35를 이미 쓰고 있었다. **열린 과제 목록만 보고 다음 번호를 고르면 이렇게 부딪힌다** — 종료 기록(`Archive/Closed-Tasks.md`)과 열린 PR까지 훑고 번호를 잡아야 한다.
- **[FE] PR #50(FE-29) 재리뷰 — 로딩 가드가 새 회귀를 만들어 되돌리고, 레이스 컨디션은 FE-32로 분리**: 사용자 요청으로 `/code-review`를 두 번(사용자가 로컬에서 하나, 내가 별도로 하나) 더 돌렸다. 어제 `/code-review`가 지적해 추가했던 `disabled={unavailable || loading}`이 **그 자체로 문제였다** — ① 포커스된 버튼이 `disabled`로 바뀌는 순간 브라우저가 강제로 blur시켜, 키보드 사용자가 Enter로 문서를 열자마자 포커스를 잃고 화면 맨 위부터 다시 Tab해야 했다(line-by-line 각도 발견). ② `loading` state는 렌더 이후에나 반영돼 같은 틱 안의 연타는 애초에 못 막는 반쪽 가드였다(efficiency 각도 발견 — "동기적 가드가 필요하다"). ③ conventions 각도가 더 근본적인 지적을 했다 — 그 수정은 FE-29의 범위(버튼 전환)와 무관한 별개 결함이고, 커밋 메시지도 그 사실을 감추듯 한 문장짜리 제목만 달고 있어 「Surgical Changes」·「Semantic Commits」 둘 다 어겼다. **그래서 `disabled={unavailable}` 단독으로 되돌렸다** — FE-29를 원래 범위(순수 버튼 전환)로 좁혔다. **진짜 레이스 컨디션(altitude 각도가 더 정확히 짚었다)은 FE-32로 새로 등록**했다 — `openDocument`에 재진입 가드가 전혀 없어서, 문서 A를 연 직후 문서 B를 열면 **나중에 도착하는 응답이 무조건 이겨** A가 B보다 늦게 오면 사용자가 보고 있는 B 위로 A가 조용히 덮어쓴다(에러도 안 남). 올바른 수정은 `loadingDocId` state가 아니라 함수 진입 시 동기적으로 갱신되는 ref로 "지금 유효한 타깃"을 추적하는 것 — 이번 PR에서 시도했다가 부작용만 만든 경로와 같은 실수를 반복하지 않기 위해 별도 과제·별도 커밋으로 남긴다. 네이티브 `disabled`가 접근성 트리에서 완전히 빠지는 것(vs 기존 `aria-disabled`)·일부 Chromium이 `disabled` 버튼의 `cursor` 인라인 스타일을 무시하는 것은 검토 후 감수하기로 기록만 하고 넘겼다(전자는 이 과제가 원래 의도한 표준 패턴, 후자는 기능 영향 없는 코스메틱). 브라우저로 되돌린 상태 재확인 — 정상/비활성 카드 동작 그대로, 콘솔 에러 0건. `npm run lint` 통과.
- **[FE] FE-32 — `openDocument`에 재진입 가드 추가, 다른 문서를 연달아 열 때 낡은 응답이 화면을 덮어쓰던 문제 고침**: FE-29(PR #50) 리뷰 중 `/code-review`가 찾아 별도 등록했던 과제. `openDocument`는 클릭마다 `/api/public/[slug]`를 다시 fetch하는데 재진입을 막는 장치가 전혀 없었다 — `loadingDocId` state는 렌더 이후에나 반영돼 같은 틱의 연타를 못 막고, 문서 A를 연 직후 문서 B를 열면 **나중에 도착하는 응답이 무조건 이겨** A가 늦게 응답하면 사용자가 보고 있는 B 위로 조용히 덮어썼다. `activeDocIdRef`(함수 진입 시점에 동기적으로 갱신되는 ref)로 "지금 유효한 타깃"을 추적하도록 고쳤다 — ① 같은 문서 연타는 재진입 시점에 즉시 걸러 새 요청 자체를 안 만든다 ② 응답이 왔을 때 그 사이 활성 타깃이 바뀌었으면(다른 문서를 열었으면) `setOpenDoc`·`sendEventLogs` 둘 다 건너뛰고 조용히 버린다. **실측**(로컬 dev, 실제 이벤트에 자료 2건을 만들어 검증 — 하나는 진짜 PDF 업로드): `/api/public/[slug]` 호출 중 첫 번째만 2초 인위 지연시켜 "문서 A를 열고 곧장 문서 B를 열었는데 A가 더 늦게 응답"하는 상황을 재현했다 — 두 응답이 모두 도착한 뒤에도 뷰어 제목은 B 그대로였고, `/ops`의 `doc_view` hits도 B만 반영되고 A는 로그 자체가 안 남았다(활성 타깃이 아니라 스킵됨). 같은 문서를 3번 연타해도 `hits`가 정확히 +1만 증가하는 것도 확인(재진입 가드). 정상 경로(하나씩 열고 닫기) 회귀 없음. `npm run lint`·`npm run build`(타입 검사 포함) 통과.
- **[FE] PR 올리기 전 `/code-review` 재확인 — `activeDocIdRef`(문서 id 값 비교) 설계의 근본 결함을 찾아 토큰 방식으로 다시 설계**: 위 커밋 직후, 머지 전에 `/code-review`를 한 번 더 돌렸다. **id를 판정 값으로 쓰면 같은 문서를 A→B→A로 다시 열 때 낡은 A 요청과 새 A 요청을 구분 못 한다**는 지적이었다 — 낡은 A가 먼저 응답하면 `activeDocIdRef.current === doc.id`(A===A, 3차 클릭이 이미 ref를 A로 되돌려놔서)가 참이 돼 낡은 응답이 "최신"으로 오인·적용되고, 그 `finally`가 ref를 지운 뒤 **진짜 최신 응답(2차 A)이 도착하면 오히려 그게 "낡음"으로 판정돼 버려진다** — 방금 성공한 요청이 조용히 사라지고 화면엔 가짜 에러만 남을 수 있는 역전 시나리오였다. `activeDocIdRef`(문서 id 값)를 `requestIdRef`(호출마다 증가하는 단조 카운터)로 바꿨다 — 값이 아니라 **호출 자체를 구분하는 토큰**이라 "가장 마지막 호출인가"만 보면 되고, 같은 문서를 몇 번을 오가도 구조적으로 안전하다. **실측**: 기존 두 시나리오(A 느림→B가 이김, 3연타→+1만 증가) 재확인에 더해, **이번에 새로 A→B→A 엣지케이스를 실측했다** — 1차 A 요청만 3초 지연시키고 B·2차 A는 빠르게 응답하도록 만들어, 두 A 요청이 모두 도착한 뒤에도 화면엔 2차 A가 정상 표시되고 가짜 에러 메시지가 없는 것을 확인(이 케이스는 `activeDocIdRef` 버전으로는 실측 없이 지나쳤다면 깨진 채로 남았을 것). 같은 리뷰가 함께 찾은 "`fetchWithTimeout`이 외부에서 취소가 안 돼 버려진 요청이 8초까지 백그라운드에서 도는" 문제는 **정확성 버그가 아니라 자원 낭비**라 FE-33으로 분리 등록했다 — 여러 기능이 공유하는 `lib/api.ts` 유틸을 고치는 일이라 FE-32와 같은 커밋에 넣지 않는 게 맞다고 판단했다. `npm run lint`·`npm run build` 통과.
- **[FE] FE-31 — 추출한 브랜드 색을 참가자 화면이 실제로 쓰게 함**: `app/[slug]/page.tsx`가 서버 응답의 `theme.preset`(`{id, label, h, c}`, BE-20이 이미 내려주고 있었다)을 버리고 `PRESETS.find(...)`으로 빌트인 5종에서만 찾고 있었다 — `origin='extracted'` 커스텀 프리셋이 걸린 이벤트는 참가자 화면이 엉뚱한 빌트인 색으로 그려졌다. `data.theme.preset ?? PRESETS.find(...) ?? PRESETS[0]`으로 고쳤다 — 서버가 내려주는 `preset` 필드가 `lib/types.ts`의 `Preset` 인터페이스(`{id, label, h, c}`)와 이미 같은 모양이라 변환 없이 바로 `derive()`에 넘길 수 있었다. "스튜디오가 CRUD API에 연결 안 돼 있어 이 경로로 실제 프리셋이 저장될 일이 없다"던 낡은 주석(FE-30이 그 연결을 이미 만들어놔서 더 이상 사실이 아니었다)을 정리하고, README「알고 있는 한계」의 해당 항목도 지웠다. **실측**(로컬 dev): `POST /api/presets`가 로그인을 요구해서(BE-25) 로컬 D1에 직접 `origin='extracted'` 프리셋(hue=20 진한 레드, 빌트인 5종과 뚜렷이 다른 색)을 INSERT하고 데모 이벤트에 걸어 브라우저로 확인 — 참가자 페이지가 추출된 색 그대로 렌더되고 브랜드 라벨도 그 프리셋 이름으로 표시됐다. `preset_id`를 다시 NULL로 돌리면 빌트인 폴백("슬레이트 뉴트럴")으로 회귀 없이 렌더. 콘솔 에러 0건. `npm run lint`·`npm run build`(타입 검사 포함) 통과. **부수 발견**: 이 브랜치의 로컬 D1이 마이그레이션 `0012`~`0015`가 안 걸려 있었다(`wrangler d1 migrations apply`로 해결) — 로컬 상태가 브랜치별이 아니라 파일 하나를 공유하는데도 이전 세션에서 어느 시점엔가 놓쳤던 것으로 보인다.
- **[FE] FE-13 — 디자인 시스템 토큰 표 + `components/ui/` 공용 컴포넌트 5종, 다크모드 배지 갭을 닫았다**: 사용자가 다음 과제로 FE-13을 직접 골랐다(추천했던 FE-23 대신). **먼저 중복을 지도로 그렸다** — `lib/ui.ts`의 `pillStyle(status)`(공개/검수대기/초안/보관)·`ReportScreen.tsx`의 샘플 데이터 태그·증감 배지·`EditorScreen.tsx`의 대비비 통과/미달 배지(3곳)가 전부 `공개`(hue 145)·`검수대기`(hue 78)·`미달`(hue 28) 계열 oklch 리터럴을 **화면마다 따로 정의**하고 있었고, 전부 **다크 페어가 없었다** — [[design]] 9절이 이미 이 갭을 알려진 갭으로 적어 두고 있었다(2026-08-19). **해법은 검사가 아니라 정본화다**: `globals.css`에 `--tone-{success,warning,danger}-{bg,fg}` 3쌍을 다른 다크 페어(brand-soft 등)와 같은 변환 규칙(배경 L 0.95→0.28~0.3, 글자 L 0.4~0.5→0.76~0.8)으로 추가하고 `lib/ui.ts`의 `UI`에 노출, `pillStyle`을 완전히 걷어내고 `components/ui/Badge.tsx`(`tone`: success·warning·danger·muted·faint)로 대체했다. **컴포넌트 4개를 더 만들었다** — `Card`(surface+line+radius, padding 조절)·`SectionTitle`(제목+설명+선택적 우측 action)·`EmptyState`·`TextInput`(라벨+입력+힌트). **적용 범위**: 콘솔(상태 배지·검색 결과 0건 안내), 리포트(샘플 데이터/증감 배지, 카드 3곳), 에디터(섹션 헤더 5곳, 기본정보 입력 필드·URL 카드, 인증서 미리보기 카드, 테마 드래프트 카드, 대비비 검증 카드+배지 4곳) — 참가자 마이크로사이트는 손대지 않았다(색 분리 원칙, [[design]] 1절). **부수로 발견한 같은 갭 5곳**: URL 중복 없음 안내·각종 에러 메시지가 배지는 아니지만 같은 hue 145/28 리터럴을 다크 페어 없이 쓰고 있어 같은 톤 토큰으로 함께 교체했다. **실측**: 브라우저로 콘솔·리포트(라이트/다크)와 에디터 기본정보·테마 섹션(다크)을 확인 — 다크모드에서 배지가 파스텔로 뜨던 문제가 사라지고 라이트모드는 배지 색이 그대로임을 확인(**"픽셀 동일"은 정확하지 않아 머지 전에 정정했다** — 콘솔 상태 배지의 배경 L이 0.96·0.965에서 0.955로, 보관 배지 글자가 0.66에서 --faint 0.62로 통일되면서 미세하게 변했다. 육안 차이는 없다). **머지 전 대조에서 회귀 1건을 잡아 고쳤다** — 리포트 증감 배지가 `<Badge style={{padding}}>`만 넘겨 Badge 기본값이 그대로 적용되면서 `font-weight`가 700→650으로 얇아지고 높이가 약 19px→22px로 커졌다(서버 렌더 HTML을 두 브랜치에서 받아 확인). 같은 PR의 에디터 배지들은 `height:'auto'`를 이미 덮고 있었는데 이 한 곳만 빠진 것이라 `height:'auto'`·`fontWeight:700`을 넘기도록 수정, 검색으로 `EmptyState`도 트리거해 확인. `npm run lint`(변경 파일 기준 오류 0)·`npm run build` 통과. [[design]] 1-1·8·9절을 갱신했다.
- **[FE] FE-34 — 아젠다 편집이 "방금 저장됨"으로 표시되지만 서버엔 안 나가던 것을 정정**: 외부 다각도 리뷰(프론트/플로우/PM/개발자/사용자 관점)가 지적한 것을 `StudioProvider.tsx`에서 코드로 재확인했다 — `patchEvent`가 세션(아젠다)만 바뀐 델타는 `detailPatchToBody`가 null을 돌려줘 **애초에 서버 저장을 예약조차 하지 않는다**(주석에 이미 "PUT .../sessions은 FE-24·FE-25 몫"이라고 적혀 있었다). 그런데 `EditorScreen.tsx`의 `AgendaSection`(드래그·키보드 이동·라이브러리에서 가져오기·삭제, 5곳)은 그 사실과 무관하게 `saved: '방금 저장됨'`을 무조건 찍고 있었다 — 서버 쓰기 기능을 만드는 게 아니라 **문구만 정직하게 바꾸는** 작은 수정이라, "미리보기에만 반영됨"(드래그 중엔 "미리보기 반영 중…")으로 교체했다. **실측**: 서버 연결된 이벤트를 열어 아젠다 항목을 삭제하니 헤더 상태가 "미리보기에만 반영됨"으로 바뀌는 것 확인, 기본 정보 필드 수정은 기존 "변경 저장 중…" 흐름 그대로 회귀 없음 확인. `npm run lint`·`build` 통과. **부수 발견(FE-36으로 별도 등록, 이번엔 안 고침)**: 같은 검증 중 로컬 전용(D1 미연결) 목업 이벤트에서 기본 정보 필드를 고치면 `saved`가 "변경 저장 중…"에서 영영 안 풀리는 것을 확인했다 — `flushServerSave`가 서버 이벤트에만 걸리는데 `BasicSection`의 onChange는 그 구분 없이 같은 문구를 찍는다. 증상은 다르지만(거짓 완료 vs 영구 대기) 뿌리는 FE-34와 같다.
- **[FE] FE-35 — 에디터의 "생성될 URL"이 실제 저장된 주소와 어긋날 수 있던 것을 정정**: 외부 다각도 리뷰(프론트/플로우/PM/개발자/사용자 관점)가 지적한 것을 코드로 재확인했다 — `EditorScreen.tsx`의 `BasicSection`이 `ev.slug`(항상 존재하고, `StudioProvider.tsx`가 서버 이벤트는 절대 로컬 재계산하지 않도록 이미 지켜주는 값)를 두고 매 렌더 `autoSlug(ev.title, ev.venue, ev.date)`를 독립적으로 다시 계산해 보여주고 있었다 — 제목을 고치면 **화면에 뜨는 URL만 바뀌고 실제 공개 주소는 그대로**라, 운영자가 보는 주소와 실제로 열리는 주소가 달라질 수 있었다. 부수로 도메인도 `sympo.studio`로 하드코딩돼 있었는데 실제 배포 도메인은 BE-22 이후 `sympo.superjacob.com`이다(README 라이브 데모 링크와 대조 확인) — 함께 정정했다. **실측**: 서버 연결된 이벤트(261003)를 열어 "생성될 URL"이 콘솔 카드의 실제 슬러그와 일치함을 확인 → 제목을 크게 바꿔 실제 PATCH까지 나가도(네트워크 확인) URL 표시가 그대로임을 확인 → 원래 제목으로 되돌려 로컬 테스트 데이터 정리. `npm run lint`·`build` 통과.

## 2026-09-20
- **[FE] FE-27 — PdfViewer 언마운트 시 렌더 취소 + 재오픈 캐싱은 안 하기로 결정**: 두 하위 문제를 다뤘다. ① 페이지 렌더 effect의 cleanup이 `cancelled = true`만 두고 실제 pdf.js `RenderTask`는 취소하지 않아, 뷰어를 바로 닫아도 진행 중이던 렌더가 이미 분리된 canvas에 계속 그려질 수 있었다 — cleanup에 `renderTaskRef.current?.cancel()`을 추가했다(같은 파일이 새 렌더를 시작하기 직전에 이미 쓰던 것과 동일한 호출이라 검증된 API, 그 결과로 뜨는 `RenderingCancelledException`도 기존 catch가 이미 정상 흐름으로 처리). ② 같은 문서를 닫았다 다시 열면 `PDFDocumentProxy`를 매번 새로 받는 것을 캐시할지 검토했는데, **안 하기로 했다** — `lib/r2.ts`의 `signDocumentUrl`을 확인하니 `exp`가 호출 시각의 초 단위 타임스탬프라, `Microsite.openDocument`가 "열기 직전에 새로 받는" 서명 URL은 **매번 완전히 다른 문자열**이 된다. URL을 키로 캐시해도 참가자가 자료를 다시 여는 실제 경로에서는 절대 히트하지 않고, 정적 URL(스튜디오 미리보기)만을 위한 모듈 전역 캐시 + 수명 관리를 들이는 건 과하다고 판단. **실측**(사용자 요청으로 로컬에 실제 이벤트 "테스트행사0920"을 새로 만들어 진행 — `POST /api/events`가 이제 로그인을 요구해서(BE-13) 로컬 D1에 직접 INSERT, PDF도 실제로 PUT 업로드해 `status: 'ready'`로 만듦): ①은 뷰어를 열고 페이지 이동(`›`)을 3연타한 직후 바로 닫아 렌더가 확실히 in-flight인 채로 언마운트시켜도 콘솔에 미처리 예외·경고가 없음을 확인. ②는 같은 문서를 두 번 열어 network 탭에서 두 요청의 `exp`·`sig`가 서로 다른 것(완전히 별개의 서명 URL로 각각 새로 다운로드)을 확인 — 캐시를 안 두기로 한 근거가 실측과 정확히 일치했다. 테스트용 이벤트는 확인 후 삭제. `npm run lint` 변경 파일 기준 오류 0건.

- **[FE] FE-27 리뷰 반영 — 다운로드 도중 닫으면 요청·워커가 안 죽던 것, 그리고 캐시 안 쓰는 이유 서술을 정정**: 팀원이 브랜치를 직접 실행해 두 가지를 짚었다. **① 실제 결함**: `PdfViewer.tsx`의 문서 로드 effect가 `pdfjsLib.getDocument({ url }).promise`를 바로 await해서, **다운로드가 끝나기 전(=`.promise`가 풀리기 전)에 언마운트되면 cleanup이 취소할 대상을 못 찾았다** — `pdfRef.current`는 resolve된 뒤에만 채워지는데 cleanup은 `pdfRef.current?.loadingTask.destroy()`만 불러서 로딩 중인 경우 아무 일도 안 일어났다. `getDocument()`가 반환하는 loading task 자체를 `loadingTaskRef`에 즉시 담아 cleanup에서 무조건 `destroy()`하도록 고쳤다(resolve 전후 상관없이 같은 객체라 한 호출로 충분). **검증 방법이 한 번 틀렸다가 바로잡았다** — 처음엔 `GET /api/files/…` 라우트에 순수 `setTimeout`만 넣어 응답을 늦췄는데, 이 지연은 `AbortSignal`을 안 봐서 **클라이언트가 진짜로 요청을 취소했는지와 무관하게** 서버가 5초를 다 채우고 `200`을 찍었다(그래서 처음엔 "닫아도 요청이 안 죽는다"는 잘못된 결론에 이를 뻔했다). 지연을 `request.signal`의 `abort` 이벤트로 다시 감싸 재검증하니, 뷰어를 닫은 시점(약 800ms)에 서버 로그에 `client aborted after 825ms`가 정확히 찍혔다(닫지 않고 그냥 두면 `delay completed without abort after ~5000ms` → 정상 렌더, 회귀 없음도 함께 확인). 임시 계측 코드는 커밋 전 원복. **② 문서 정정**: "서명 URL의 `exp`가 매번 달라 URL을 키로 캐시해도 **절대** 히트하지 않는다"고 적었던 어제 기록·코드 주석이 부정확했다 — `exp`가 초 단위라 **같은 초 안에 재요청하면 우연히 같을 수 있다.** 문서 id·파일 버전을 키로 쓰는 대안도 검토는 됐다고 명시하고, 캐시를 안 두는 이유를 "URL이 절대 안 겹친다"가 아니라 "관리 비용·메모리 사용이 이 경로 무게에 비해 크다"로 다시 썼다(코드 주석·`Archive/Closed-Tasks`도 함께 정정). `npm run lint`·`npm run build`(타입 검사 포함) 통과.

- **[FE] PR #49 `/code-review high` 재검증 — 리팩터 중 생긴 진짜 회귀 1건**: PR을 열어둔 채로(머지 전) 다시 리뷰를 돌렸다. `getDocument()` 반환값을 `loadingTaskRef`에 담는 리팩터 과정에서 그 호출 자체가 `try` 밖으로 빠져나가 있었다 — `getDocument()`가 동기적으로 던지면(정상 흐름은 아니지만 원래 코드는 이 경로까지 `try`로 덮고 있었다) `catch`가 못 잡아 `setState('error')`가 영영 안 불리고 뷰어가 "불러오는 중…"에 멈춘다. `getDocument()` 호출을 다시 `try` 안으로 옮겨 원래 안전성을 복구했다. 같은 리뷰가 후보로 낸 "`RenderTask.cancel()`을 두 번 부를 수 있다"는 별도 검증에서 **기각**됐다 — pdf.js 6.3.289 소스를 직접 추적해 두 번째 `cancel()`의 모든 단계가 멱등 가드(이미 취소된 플래그, `streamReader` null 체크, 이미 정착한 promise reject 무시)로 no-op임을 확인. **실측 재확인**: 로컬 D1에 문서를 다시 만들어(이번엔 UTF-8 파일로 curl 본문을 보내 인코딩 문제 재발 방지) 브라우저로 열람 → 정상 렌더·콘솔 에러 0건. `npm run lint`·`npm run build` 통과. 테스트 문서는 삭제.
- **[FE] FE-29 — 자료 카드를 네이티브 `<button>`으로 리팩터**: `Microsite.tsx`의 자료 카드가 `role="button"`·수동 `tabIndex`·직접 구현한 Enter/Space `onKeyDown`으로 버튼 의미를 재구현하고 있던 것을 `<button type="button" disabled={unavailable}>`로 교체했다. 카드 룩은 `width: 100%`·`textAlign: 'left'`·`appearance: 'none'` 리셋만으로 유지됐다(전역 `button { font-family: inherit }`가 이미 있어 폰트 리셋은 불필요). **`/code-review`가 리팩터 자체의 문제는 못 찾고, 대신 인접한 진짜 갭 하나를 찾았다** — 새 `disabled`가 `unavailable`만 보고 `loading`(서명 URL을 다시 받는 중)은 안 봐서, 로딩 중 연타하면 `openDocument`가 중복 호출돼 네트워크 요청이 겹치고 `setOpenDoc`·`setDocError`·`setLoadingDocId` 쓰기가 경합할 수 있었다. **이 diff가 만든 회귀는 아니다** — 기존 `<div role="button">`도 로딩 중 클릭을 막은 적이 없었다. 다만 `disabled` 로직을 이번에 처음 만지는 김에 `unavailable || loading`으로 완전하게 만들고 `cursor`도 같이 맞췄다. **실측**: ready·pending 자료 둘을 만들어(하나는 실제 PDF 업로드) 브라우저로 비교 — 비활성 카드는 클릭 무반응 + `.focus()` 호출 자체가 거부됨(네이티브 disabled 확인), 정상 카드는 클릭·키보드 Enter 둘 다 정상 오픈. `/api/public/[slug]` 응답을 1.5초 인위 지연시켜 로딩 중 연타를 재현 — 클릭 100ms 뒤 이미 `disabled: true`이고 두 번째 `.click()`은 네이티브 disabled라 이벤트 자체가 안 뜸(중복 요청 0건). 콘솔 에러 0건. `npm run lint`·`npm run build` 통과.

## 2026-09-17
- **[PROJ] README를 168줄 → 96줄(62% 감소)로 압축하고, 그 과정에서 낡은 서술 8곳을 잡았다**: 사용자가 "핵심만 남기고 전부 삭제"를 요청했다. **압축 기준은 "이 문서가 포트폴리오의 본체"라는 원래 목적**이었다 — 이 저장소를 남과 구별짓는 것은 CRUD 구현이 아니라 **현장에서 겪은 문제 관찰**이므로, 「문제」 절의 두 층 구조와 "관찰/가설 분리"는 압축하되 **뼈대를 남겼고**, 나머지를 잘랐다. 없앤 것: 「관찰과 가설을 갈라 둡니다」 절(한 문단으로 흡수) · 「다시 만들면서 바꾼 것」(설계 판단과 중복) · 「화면」 표 2개(상태 표 1개로) · 「데이터에 대하여」(상단 고지로 흡수) · 설계 판단 표 7행 → 5행 · 기술 선택 4문단 → 불릿 4개. **압축보다 중요했던 건 사실 정정이다** — 읽어보니 README가 현재와 8곳에서 어긋나 있었다: ① "API 18개"(실제 20) ② "테스트 23건"(실제 99) ③ "운영자 스튜디오가 서버에 연결된 적이 없다 — 서버 호출이 한 줄도 없다"(FE-30이 기본 정보를 붙였다. 코드로 확인: `StudioProvider`에 fetch 3건) ④ "목록·상세·운영 지표 조회와 프리셋 쓰기는 인가 검사가 없다"(**BE-13·24·25가 전부 닫았다** — `/ops`·`/events`·`/presets` 라우트에서 직접 확인) ⑤ "강의자료 뷰어에 포커스 트랩이 없다"(FE-26이 닫았다) ⑥ "맞는 층은 엣지이며 커스텀 도메인이 필요하다"(도메인이 생겼고 **기각했다** — ADR 0013 링크로 대체) ⑦ 화면 표의 ⚠️ 목업 4개(에디터는 이제 부분 연결) ⑧ 배포 주소. **"의도적으로 안 하는 것"이라는 항목을 새로 만들었다** — 엣지 rate limiting 기각처럼 *못 한 것이 아니라 안 하기로 한 것*은 한계와 성격이 달라서, 섞어 두면 읽는 사람이 미완으로 오해한다. 링크 5개 전부 유효 확인.
- **[PROJ] 위키를 코드와 대조해 표류 6곳을 잡았다 — 그중 하나는 FE가 못 보고 있던 엔드포인트다**: 머지할 것은 없었고(main, 변경 0, 미머지 0) 위키 정합성만 봤다. **가장 실질적인 발견**: `app/api`의 라우트 20개를 [[API-Guide-FE]]와 하나씩 대조하니 **`GET /api/public/[slug]/report`가 문서에 없었다.** BE-24가 `/ops`를 소유자 전용으로 잠그면서 갈라 낸 참가자용 공개 집계 경로인데, 문서에 없으면 **FE는 계속 운영자 경로(`/ops`)를 불러 404를 맞는다.** 요약표와 상세 절을 더하고 *"참가자 리포트는 이걸 쓴다, `/ops`는 소유자 전용이라 404"*를 명시했다 — 인증이 필요 없는 이유(담기는 건 집계 수치뿐이고 애초에 공개된 행사의 것)와 경계가 소유권이 아니라 **공개 상태**라는 것도 함께. 이제 **20/20 문서화**됐다. **나머지 표류 5곳**: ① 테스트 수 — 어제 `rate-policy.test.ts`를 추가하면서 "11파일 83건"이 "12파일 99건"으로 어긋났다(Context 2곳·Status 1곳). **내가 만든 표류를 내가 못 잡은 것**이라, 다음부터 테스트 파일을 늘리면 그 수치가 박힌 곳을 함께 본다. ② [[Onboarding-FE]]·[[Summaries/Next]]가 **종료·기각된 BE 4건(BE-22·27·28·32)을 여전히 "할 일"로** 들고 있었다 — 쉬운 것/고민 필요한 것 표에서 빼고 FE로 채웠으며, "BE 열린 과제 0건"을 명시했다. ③ `Next-Tasks`의 「전체 조망」 날짜가 09-15에서 멈춰 있었다. ④ 같은 파일의 ⚠️ 절이 **이제 전부 종료된 BE-24~29를 살아있는 경고처럼** 적고 있었다 — 교훈만 남기고 상태를 갱신하면서, 이번에 겪은 **두 번째 사고(완결된 구현이 미머지 브랜치에 묵어 목록과 어긋남, 마이그레이션 0007·0011 결번도 같은 원인)**를 나란히 적었다. 두 교훈이 같은 자리에 있어야 다음에 둘 다 읽힌다.
- **[PROJ] BE-32 기각 — 제공자별 별개 계정을 채택된 모델로 확정했다. BE 열린 과제가 0건이 됐다**: 사용자 판단이 출발점이었다 — *"이메일이 달라지거나 다른 계정으로 로그인해야 되는 상황도 있어서 굳이 하나의 계정으로 안 묶어도 될 것 같다."* 검토해보니 근거가 더 있었다: ① 제공자가 Google 하나뿐이라 **갈릴 일 자체가 아직 없다**(존재하지 않는 문제였다) ② **연결을 안 하면 탈취 표면이 0이다** — 연결 기능은 "이 `sub`를 이 계정에 붙여도 되는가"라는 판정을 하나 더 만드는데 그게 틀리면 곧 계정 탈취이고, **그게 BE-26이 닫은 바로 그 구멍**이다 ③ "이메일이 바뀌는 상황"은 BE-26·BE-30이 **이미 처리했다**(이메일은 표시용, 겹쳐도 별개 계정) ④ 강제로 묶는 건 제품이 "이 둘은 같은 사람"을 단정하는 것이라 업무용/개인용 분리를 방해한다. **과제를 지우는 것으로 끝내지 않은 이유**가 중요하다 — [[0011-account-link-key]]의 「감수하는 것」이 *"옳은 해법은 명시적 연결 흐름"*이라 적고 「재검토 트리거」가 BE-32를 가리키고 있어서, 그냥 지우면 **트리거가 죽은 링크로 남고 나중에 누가 "같은 사람이 갈리네"에서 가장 쉬운 해법인 이메일 매칭을 되살릴** 수 있다(그 유혹이 정확히 그 문서가 막으려고 쓰인 것이다). 그래서 두 항목을 **개정**하고 「개정」 절을 새로 달아 **갈리는 것은 결함이 아니라 의도**임을 박았다. 트리거도 바꿔 적었다 — 두 번째 제공자를 붙일 때 할 일은 연결 UI가 아니라 `upsertUser`가 `provider`를 하드코딩된 `'google'` 대신 인자로 받게 하는 것뿐이다. **감수하는 것**: Google 계정을 잃으면 그 계정의 이벤트에 못 들어간다 — 다만 이건 연결이 아니라 **「계정 복구」라는 다른 문제**이고(둘을 이어놔도 둘 다 잃으면 같다) 운영자 데이터가 실제로 쌓이는 단계에 오면 과제로 세운다. **BE 섹션이 비었지만 헤더는 남겼다** — 훅이 `## 열린 과제` 섹션에서 `### BE-`를 추출하므로 헤더를 지우면 다음 BE 과제가 주입에서 통째로 빠진다. 열린 과제 13건(Next-Task 1 · FE 12 · **BE 0**), 종료 50건.
- **[PROJ] 정본 주소를 `sympo.superjacob.com`으로 옮기고, 그 김에 낡은 연표를 이어 썼다**: 옛 `.workers.dev` 주소가 9곳에 있었는데 **전부 바꾸지 않았다.** 바꾼 것은 **현재 상태·운영 안내 4곳**(README·[[API-Guide-FE]]·[[Context]]·[[Summaries/Status]])이고, **유지한 것은 그때의 사실**이다 — `Archive/` 2곳(규칙상 원문 그대로), [[0008-rate-limit-counter]]의 *"지금 배포는 workers.dev이고 거기엔 WAF를 못 건다"*(**기각 근거 문장이라 바꾸면 0013의 부분 개정이 안 읽힌다**), [[Summaries/History]]의 BE-2 항목. API-Guide와 Context에는 **구 주소도 살아 있다**는 것을 명시했다 — 밖으로 나간 링크를 죽이지 않으려고 `workers_dev: true`로 둘 다 유지 중이다. **그 김에 [[Summaries/History]]가 9/14에서 멈춰 있는 것을 발견했다.** ⑩(9/15~9/17)을 추가하고 — 스튜디오↔D1 연결·인가 경계·계정 연결 키·업로드 예약·커스텀 도메인·Q&A 한도 — **「되돌아보면」의 낡은 결론을 고쳤다**: *"아직 안 메운 가장 큰 구멍은 스튜디오↔D1 연결"*이 더는 사실이 아니다(FE-30이 닫았다). 대신 이번에 실제로 드러난 패턴을 적었다 — **"이미 다 해놓고 과제 목록엔 열려 있는" 상태의 반복**(BE-15·BE-27이 완결된 브랜치로 묵었고, 마이그레이션 0007·0011 결번도 같은 원인)과 그래서 생긴 규칙(착수 전 `git branch --no-merged main`).
- **[BE] Q&A 창 한도 3→10, IP 창 20→60 — 하루 한도는 건드리지 않았다**: 사용자의 "네트워크 보안이 과한 것 같다"는 판단을 받아 Q&A 정책만 완화했다. 요점은 **노출의 천장은 하루 한도이고 마찰은 창 한도에서 온다**는 분리다 — 창을 올려도 **하루에 쓸 수 있는 총량은 1도 늘지 않는다**(브라우저 30·IP 300 그대로). 3이 문제였던 이유는 남용이 아니라 **데모 마찰**이다: 눌러보는 사람이 질문을 연달아 던지면 4번째에서 429를 맞고 "고장났다"로 읽는다. 0006이 재검토 트리거에 적어둔 "정상 사용에서 429가 관측되면 상한 재조정"에 해당한다. **IP 창을 함께 올린 이유**가 중요하다 — 브라우저 창만 10으로 올리면 열심히 눌러보는 **한 사람이 행사장 창 한도(20)의 절반을 혼자 먹는다.** 0006이 IP 창 20을 "120명 행사 피크를 막지 않는 선"으로 잡았으므로 두 값은 함께 움직여야 한다. **실측**: 같은 토큰 연속 전송 → 10건 통과·11번째부터 429(문구가 상수를 보간해 "60초에 10건"으로 자동 갱신), 하루 카운터를 29로 맞추고 창을 비운 뒤 → 30번째 통과·31번째 "오늘 남길 수 있는 질문 수(30건)를 모두 사용했습니다" — **하루 천장 불변 확인.** **다른 정책은 안 건드렸다** — survey(창 60)·logs(창 60)·events(창 25)·upload(5분 200MB)는 이미 넉넉하고 근거도 적혀 있어, 빡빡한 건 Q&A 하나뿐이었다. 값이 아니라 **값들 사이의 관계**(창 ≤ 하루, IP ≥ 브라우저, 창 길이 > 0)를 `lib/rate-policy.test.ts` 16건으로 고정했다 — ADR 없이 바뀌는 자리라 관계가 깨지면 증상이 조용하다. `lint`·`test`(99건)·`build` 통과.
- **[BE] BE-22 종료 — 커스텀 도메인은 붙이고, 엣지 rate limiting은 기각했다**: 사용자가 `superjacob.com`을 보유 중이라 **선행 조건이 구매 없이 풀렸다** — 이미 Cloudflare 네임서버(keira/keanu)를 쓰고 있어 존이 존재했다. `sympo.superjacob.com`을 `routes` + `custom_domain: true`로 붙였고 `/intro`·`/console`·`/api/presets`·`/api/public/[slug]` 전부 200 실측. **사고 하나**: `routes`를 지정하면 wrangler가 **workers.dev 서브도메인을 기본으로 끈다**. 모르고 배포해 기존 주소가 전 경로 404(`error code: 1042`)가 됐고, 배포 후 실측에서 잡아 `workers_dev: true`로 되살렸다 — README·위키 9곳이 그 주소를 가리키고 있어 두 주소가 함께 살아 있어야 한다. 사유를 설정 파일 주석에 남겼다. **엣지 규칙은 기각했다**([[0013-edge-rate-limiting-rejected]]). 0002가 지시한 대로 공식 문서로 무료 플랜 제약을 재확인했더니 **Rate Limiting Rule의 카운팅 기준이 IP 단독**이었다 — [[0006-rate-limit-key]]가 "행사장은 단일 egress IP라 방 전체가 잠긴다"며 기각한 바로 그 방식이라, 걸면 **정상 행사를 깨뜨리는 경로를 엣지에서 되살린다.** 산술로도 양립하지 않는다: 참가자 100명 폴링이 한 IP 10초당 200~330 요청인데, 그걸 안 막는 임계값(10초당 1000)은 **초당 100 요청을 통과시켜 17분이면 Workers 일일 한도 10만을 태운다.** Managed Challenge는 IP 문제를 피하지만 0006이 CAPTCHA를 기각한 이유(고령 사용자 마찰, 이 제품이 푸는 문제가 정확히 그것이다)가 그대로 적용돼 참가자 경로에는 안 건다. **남긴 판단**: 0008의 "엣지가 맞는 층"은 유효하고 **무료 플랜에서는 실현되지 않는다**는 것을 확정했다(0008에 부분 개정 포인터). 얻은 것은 **존 확보** — 플랜을 올리면 바로 걸 수 있다. 착수 배경에 사용자의 "네트워크 보안을 좀 느슨하게 가도 된다, 지금 과한 것 같다"는 판단이 있었고, 실제로 문서를 확인해보니 그 방향이 맞았다 — 다만 **업로드 상한(R2에 결제 수단이 붙어 있다)과 인가 경계는 같은 층이 아니라 그대로 둔다.**
- **[PROJ] 브랜치 26개 → 1개로 정리 + 원격 마이그레이션 0015 적용, 그리고 [[Context]]의 낡은 수치 정정**: 로컬 25개·원격 10개를 지워 **로컬은 `main` 하나, 원격은 `main` + 폐기 예정 2개**만 남겼다. 지우기 전에 **원격 사본이 없는 로컬 13개가 전부 main에 머지됐는지 하나씩 확인**했고(커밋이 main 히스토리에서 도달 가능해야 잃는 것이 없다), 머지된 것은 `git branch -d`(미머지면 git이 거부한다)로, 대체된 2개만 origin 사본을 확인한 뒤 `-D`로 지웠다. 삭제 전 SHA를 파일로 남겼다. **남긴 2개**(`feat/be-12-auth`·`docs/pr9-cross-review`)는 미머지지만 **머지하면 main이 깨진다** — 전자는 이미 적용된 `0010_auth.sql`과 같은 테이블을 만드는 `0007_auth.sql`을 되살리고 인증 코드를 2026-09-07로 되돌리며, 후자는 `Next-Tasks.md`를 2026-08-31 파일 전체로 덮어 **종료된 FE-1~9·BE-2~16을 열린 과제로 부활**시킨다. 둘 다 내용은 이미 main에 있다(cherry-pick / 종료 기록). 원격 브랜치 삭제 전에 **열린 PR이 걸려 있는지 확인**했다 — #46은 huni2 포크의 브랜치라 무관했다. 원격 D1에 `0015`를 적용해 **로컬·원격이 0001~0015로 동기화**됐다(백필 대상 0건 — 원격 `documents`가 0건이다. 백업은 뜨고 진행, 적용 후 행 수 7종·운영 엔드포인트 5종 무변화 확인). **[[Context]]를 함께 고쳤다** — 테스트 "23건 / 3파일"이 실제로는 **83건 / 11파일**이었고 마이그레이션 서술도 `0010`에서 멈춰 있었다. 결번을 **0007 하나에서 0007·0011 둘로** 고치며 두 사고가 같은 원인(미머지 브랜치에 묵는 사이 뒤 번호가 먼저 적용됨)임을 적었다.
- **[PROJ] 미머지 로컬 브랜치를 훑어 BE-27·BE-28이 이미 구현돼 있던 것을 발견 — 그리고 BE-28을 중복 구현했다**: 사용자가 "작업 완료된 항목은 따로 로컬 브랜치가 있는 걸로 안다"고 짚어 `git branch --no-merged main`을 봤더니, **`be-27-document-status`와 `be-28-demo-theme-reset`에 완결된 구현이 있었다.** BE-28은 내가 그날 처음부터 다시 구현한 뒤였고, 두 구현이 `DEMO_THEME` 상수 접근·컬럼 목록·가짜 D1의 `demoExists: false` 확장까지 **독립적으로 같은 결론에 도달**해 있었다(코드 결과물은 동등). **착수 전에 브랜치를 확인하지 않은 것이 원인**이다 — BE-15도 같은 이유로 종료 기록과 열린 과제 양쪽에 있었고, 이 저장소에서 반복되는 패턴이다. 앞으로 과제 착수 전 `git branch --no-merged main`을 먼저 본다. 두 브랜치를 머지했다: BE-28 쪽은 main에 동등 구현이 이미 있어 **`eslint.config.mjs`의 `.wrangler/**` 무시 규칙과 더 엄격한 테스트 단언(`= ?` 바인딩까지 고정)만** 취했고, 위키는 브랜치가 BE-26 시점에서 갈라져 있어(BE-24·25·28·30을 열린 과제로 되살리려 했다) 전부 main 쪽을 택했다.
- **[BE] BE-27 마이그레이션 번호를 0011 → 0015로 옮겼다**: 이 마이그레이션이 미머지 브랜치에 묵는 사이 **0012~0014가 먼저 적용됐다**(원격 포함). 앞 번호를 두면 "적용된 0014 뒤에 0011이 적용되는" 이력이 남는다 — 내용이 앞 셋과 독립이라 순서 의존은 없다. **BE-12가 0007→0010으로 옮긴 것과 같은 판단**이고, 사유를 마이그레이션 파일 머리에 남겼다. 덧붙여, 앞서 내가 "설명 없는 결번"이라고 보고했던 `0011`의 정체가 **바로 이 브랜치**였다 — 결번이 아니라 묵고 있던 번호였다.
- **[BE] BE-27 — 자료 status를 검사하는 대신 `r2_key`에서 파생시켰다**: `PUT …/documents`가 본문의 `status`를 그대로 저장하는데 검사는 **신규 생성에만** 있었다(`id === null && status === 'ready'` → 400). 기존 항목 수정 경로에는 없어서 **`r2_key`가 NULL인 자료를 메타 저장 한 번으로 `ready`로 올릴 수 있었다** — 참가자 화면에 **열 수 없는 자료가 "준비됨"으로** 뜬다. FE-6이 `unavailable = pending || !url` 게이트로 막아둔 것은 증상을 가린 것이지 원인이 아니고, 서버가 일관성을 보장하지 않으면 다른 소비자(운영자 화면·리포트)가 같은 함정을 각자 다시 밟는다. **검사를 한 군데 더 넣는 대신 값의 출처를 없앴다** — `validateDocumentsBody`가 `status`를 아예 읽지 않고, 라우트가 `UPDATE … SET status = CASE WHEN r2_key IS NULL THEN 'pending' ELSE 'ready' END`으로 같은 문에서 파생시킨다(신규 INSERT는 항상 `'pending'`). **검사는 잊을 수 있지만 파생은 잊을 수 없다** — 예전 실패가 정확히 "한쪽 경로에만 검사를 넣었다"였다. **이미 어긋나 있는 행은 코드로 안 고쳐지므로** 마이그레이션 `0015_document_status_from_file.sql`을 함께 넣었다(파생과 같은 식으로 정정, 데이터는 안 지운다 — `pending`은 결함 표시가 아니라 "연자 자료가 아직 안 왔다"는 이 테이블의 정상 상태다). **실측**: 파일 없는 자료에 `ready`를 실어 PUT → 응답 `pending` / 업로드 후 `ready` 회귀 없음 / 그 뒤 `pending`을 실어 이름만 바꿔 저장 → **`ready` 유지 + 이름 반영** / 새 자료에 `ready` → 400이 아니라 `pending`으로 생성 / 마이그레이션은 어긋난 행을 일부러 만들어 적용해 정정 확인. 테스트 데이터는 정리했다. `lib/agenda.test.ts` 신설 4건, `npm run lint`·`test`(32건)·`build` 통과. **FE 계약 변화**: `status`를 보내도 무시된다 — API-Guide-FE에 "응답의 `status`를 그대로 쓰라"로 명시했다.
